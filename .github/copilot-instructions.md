

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.10 -->
<!-- Updated: 2026-07-18T08:05:24.392Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
backend/venv/lib/python3.14/site-packages/httpx/_status_codes.py ← __future__
backend/venv/lib/python3.14/site-packages/httpx/_urlparse.py ← __future__, _exceptions, ipaddress, idna
backend/venv/lib/python3.14/site-packages/httpx/_urls.py ← __future__, _types, _urlparse, _utils, idna
backend/venv/lib/python3.14/site-packages/httpx/_utils.py ← __future__, _types, ipaddress
backend/venv/lib/python3.14/site-packages/httpx/_transports/asgi.py ← __future__, _models, _types, base
backend/venv/lib/python3.14/site-packages/httpx/_transports/base.py ← __future__, types, _models
backend/venv/lib/python3.14/site-packages/httpx/_transports/default.py ← __future__, types, _config, _exceptions, _models
backend/venv/lib/python3.14/site-packages/httpx/_transports/mock.py ← __future__, _models, base
backend/venv/lib/python3.14/site-packages/httpx/_transports/wsgi.py ← __future__, _models, _types, base
backend/venv/lib/python3.14/site-packages/cryptography/exceptions.py ← __future__, cryptography
backend/venv/lib/python3.14/site-packages/cryptography/fernet.py ← __future__, cryptography
backend/venv/lib/python3.14/site-packages/cryptography/hazmat/_oid.py ← __future__, cryptography
backend/venv/lib/python3.14/site-packages/cryptography/utils.py ← __future__, types
backend/venv/lib/python3.14/site-packages/cryptography/x509/base.py ← __future__, cryptography
backend/venv/lib/python3.14/site-packages/cryptography/x509/certificate_transparency.py ← __future__, cryptography
backend/venv/lib/python3.14/site-packages/cryptography/x509/extensions.py ← __future__, cryptography, ipaddress
backend/venv/lib/python3.14/site-packages/cryptography/x509/general_name.py ← __future__, cryptography, ipaddress
backend/venv/lib/python3.14/site-packages/cryptography/x509/name.py ← __future__, cryptography
backend/venv/lib/python3.14/site-packages/cryptography/x509/ocsp.py ← __future__, cryptography
backend/venv/lib/python3.14/site-packages/realtime/_async/channel.py ← __future__, typing_extensions, realtime, message, transformers
backend/venv/lib/python3.14/site-packages/realtime/_async/client.py ← pydantic, websockets, exceptions, message, transformers
backend/venv/lib/python3.14/site-packages/realtime/_async/presence.py ← types
backend/venv/lib/python3.14/site-packages/realtime/_async/push.py ← __future__, typing_extensions, message, types
backend/venv/lib/python3.14/site-packages/realtime/_sync/channel.py ← __future__, realtime
backend/venv/lib/python3.14/site-packages/realtime/_sync/client.py ← channel
backend/venv/lib/python3.14/site-packages/realtime/_sync/presence.py ← types
backend/venv/lib/python3.14/site-packages/realtime/message.py ← pydantic, typing_extensions, types
backend/venv/lib/python3.14/site-packages/realtime/types.py ← __future__, pydantic, typing_extensions
backend/venv/lib/python3.14/site-packages/fastapi/_compat/shared.py ← fastapi, pydantic, starlette, types
backend/venv/lib/python3.14/site-packages/fastapi/_compat/v2.py ← fastapi, pydantic, pydantic_core
backend/venv/lib/python3.14/site-packages/fastapi/applications.py ← annotated_doc, fastapi, starlette, typing_extensions
backend/venv/lib/python3.14/site-packages/fastapi/background.py ← annotated_doc, starlette, typing_extensions
backend/venv/lib/python3.14/site-packages/fastapi/concurrency.py ← anyio, starlette
backend/venv/lib/python3.14/site-packages/fastapi/datastructures.py ← annotated_doc, pydantic, starlette
backend/venv/lib/python3.14/site-packages/fastapi/dependencies/models.py ← fastapi
backend/venv/lib/python3.14/site-packages/fastapi/dependencies/utils.py ← fastapi, pydantic, starlette, typing_inspection
backend/venv/lib/python3.14/site-packages/fastapi/encoders.py ← ipaddress, types, annotated_doc, fastapi, pydantic
backend/venv/lib/python3.14/site-packages/fastapi/exception_handlers.py ← fastapi, starlette
backend/venv/lib/python3.14/site-packages/fastapi/exceptions.py ← annotated_doc, pydantic, starlette
backend/venv/lib/python3.14/site-packages/fastapi/middleware/asyncexitstack.py ← starlette
backend/venv/lib/python3.14/site-packages/fastapi/openapi/docs.py ← annotated_doc, fastapi, starlette
backend/venv/lib/python3.14/site-packages/fastapi/openapi/models.py ← fastapi, pydantic, typing_extensions
backend/venv/lib/python3.14/site-packages/fastapi/openapi/utils.py ← fastapi, pydantic, starlette
backend/venv/lib/python3.14/site-packages/fastapi/param_functions.py ← annotated_doc, fastapi, pydantic, typing_extensions
backend/venv/lib/python3.14/site-packages/fastapi/params.py ← fastapi, pydantic, typing_extensions, _compat, datastructures
backend/venv/lib/python3.14/site-packages/fastapi/responses.py ← fastapi, starlette, typing_extensions, importlib
backend/venv/lib/python3.14/site-packages/fastapi/routing.py ← contextvars, annotated_doc, anyio, fastapi, starlette
backend/venv/lib/python3.14/site-packages/fastapi/security/api_key.py ← annotated_doc, fastapi, starlette
backend/venv/lib/python3.14/site-packages/fastapi/security/base.py ← fastapi
backend/venv/lib/python3.14/site-packages/fastapi/security/http.py ← annotated_doc, fastapi, pydantic, starlette
backend/venv/lib/python3.14/site-packages/fastapi/security/oauth2.py ← annotated_doc, fastapi, starlette
backend/venv/lib/python3.14/site-packages/fastapi/security/open_id_connect_url.py ← annotated_doc, fastapi, starlette
backend/venv/lib/python3.14/site-packages/fastapi/sse.py ← annotated_doc, pydantic, starlette
backend/venv/lib/python3.14/site-packages/fastapi/utils.py ← fastapi, pydantic, _compat
backend/venv/lib/python3.14/site-packages/supabase_functions/__init__.py ← __future__, _async, _sync, utils
backend/venv/lib/python3.14/site-packages/supabase_functions/_async/functions_client.py ← httpx, yarl, errors, utils, version
backend/venv/lib/python3.14/site-packages/supabase_functions/_sync/functions_client.py ← httpx, yarl, errors, utils, version
backend/venv/lib/python3.14/site-packages/supabase_functions/errors.py ← __future__
backend/venv/lib/python3.14/site-packages/supabase_functions/utils.py ← httpx
backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_admin_api.py ← __future__, httpx, helpers, types, gotrue_admin_mfa_api
backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_admin_mfa_api.py ← types
backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_admin_oauth_api.py ← types
backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_base_api.py ← __future__, httpx, pydantic, typing_extensions, constants
backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_client.py ← __future__, httpx, jwt, typing_extensions, constants
backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_mfa_api.py ← types
backend/venv/lib/python3.14/site-packages/supabase_auth/_async/storage.py ← __future__
backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_admin_api.py ← __future__, httpx, helpers, types, gotrue_admin_mfa_api
backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_admin_mfa_api.py ← types
backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_admin_oauth_api.py ← types
backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_base_api.py ← __future__, httpx, pydantic, typing_extensions, constants
backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_client.py ← __future__, httpx, jwt, typing_extensions, constants
backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_mfa_api.py ← types
backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/storage.py ← __future__
backend/venv/lib/python3.14/site-packages/supabase_auth/errors.py ← __future__, typing_extensions
backend/venv/lib/python3.14/site-packages/supabase_auth/helpers.py ← __future__, httpx, pydantic, constants, errors
backend/venv/lib/python3.14/site-packages/supabase_auth/types.py ← __future__, pydantic, typing_extensions
backend/venv/lib/python3.14/site-packages/storage3/__init__.py ← __future__, storage3
backend/venv/lib/python3.14/site-packages/storage3/_async/analytics.py ← httpx, types, request
backend/venv/lib/python3.14/site-packages/storage3/_async/bucket.py ← __future__, httpx, yarl, exceptions, types
backend/venv/lib/python3.14/site-packages/storage3/_async/client.py ← __future__, httpx, storage3, version, analytics
backend/venv/lib/python3.14/site-packages/storage3/_async/file_api.py ← __future__, httpx, yarl, constants, exceptions
backend/venv/lib/python3.14/site-packages/storage3/_async/request.py ← httpx, pydantic, yarl, exceptions, types
backend/venv/lib/python3.14/site-packages/storage3/_async/vectors.py ← __future__, httpx, yarl, exceptions, types
backend/venv/lib/python3.14/site-packages/storage3/_sync/analytics.py ← httpx, types, request
backend/venv/lib/python3.14/site-packages/storage3/_sync/bucket.py ← __future__, httpx, yarl, exceptions, types
backend/venv/lib/python3.14/site-packages/storage3/_sync/client.py ← __future__, httpx, storage3, version, analytics
backend/venv/lib/python3.14/site-packages/storage3/_sync/file_api.py ← __future__, httpx, yarl, constants, exceptions
backend/venv/lib/python3.14/site-packages/storage3/_sync/request.py ← httpx, pydantic, yarl, exceptions, types
backend/venv/lib/python3.14/site-packages/storage3/_sync/vectors.py ← __future__, httpx, yarl, exceptions, types
backend/venv/lib/python3.14/site-packages/storage3/exceptions.py ← pydantic, utils
backend/venv/lib/python3.14/site-packages/storage3/types.py ← __future__, pydantic, typing_extensions
backend/venv/lib/python3.14/site-packages/storage3/utils.py ← deprecation, httpx, version
backend/venv/lib/python3.14/site-packages/postgrest/_async/client.py ← __future__, deprecation, httpx, yarl, base_client
backend/venv/lib/python3.14/site-packages/postgrest/_async/request_builder.py ← __future__, httpx, pydantic, typing_extensions, yarl
backend/venv/lib/python3.14/site-packages/postgrest/_sync/client.py ← __future__, deprecation, httpx, yarl, base_client
backend/venv/lib/python3.14/site-packages/postgrest/_sync/request_builder.py ← __future__, httpx, pydantic, typing_extensions, yarl
backend/venv/lib/python3.14/site-packages/postgrest/base_client.py ← __future__, httpx, yarl, utils
backend/venv/lib/python3.14/site-packages/postgrest/base_request_builder.py ← __future__, httpx, pydantic, yarl, base_client
backend/venv/lib/python3.14/site-packages/postgrest/exceptions.py ← pydantic
backend/venv/lib/python3.14/site-packages/postgrest/types.py ← __future__, httpx, pydantic, typing_extensions, yarl
backend/venv/lib/python3.14/site-packages/postgrest/utils.py ← __future__, deprecation, httpx, pydantic, yarl
backend/venv/lib/python3.14/site-packages/supabase/_async/auth_client.py ← httpx, supabase_auth
backend/venv/lib/python3.14/site-packages/supabase/_async/client.py ← httpx, postgrest, realtime, storage3, supabase_auth
backend/venv/lib/python3.14/site-packages/supabase/_sync/auth_client.py ← httpx, supabase_auth
backend/venv/lib/python3.14/site-packages/supabase/_sync/client.py ← httpx, postgrest, realtime, storage3, supabase_auth
backend/venv/lib/python3.14/site-packages/supabase/lib/client_options.py ← httpx, postgrest, storage3, supabase_auth, supabase_functions
backend/venv/lib/python3.14/site-packages/pkg_resources/__init__.py ← pkgutil, pkg_resources, types, zipfile, zipimport
backend/venv/lib/python3.14/site-packages/pkg_resources/_vendor/typing_extensions.py ← types
backend/venv/lib/python3.14/site-packages/pkg_resources/_vendor/zipp.py ← posixpath, zipfile
backend/venv/lib/python3.14/site-packages/pkg_resources/extern/__init__.py ← importlib
backend/venv/lib/python3.14/site-packages/setuptools/__init__.py ← distutils, ., depends, discovery, dist
backend/venv/lib/python3.14/site-packages/setuptools/_core_metadata.py ← distutils, ., extern, warnings
backend/venv/lib/python3.14/site-packages/setuptools/_entry_points.py ← errors, extern, _importlib, _itertools
backend/venv/lib/python3.14/site-packages/setuptools/_imp.py ← importlib
backend/venv/lib/python3.14/site-packages/setuptools/_itertools.py ← setuptools
backend/venv/lib/python3.14/site-packages/setuptools/_normalization.py ← extern
backend/venv/lib/python3.14/site-packages/setuptools/_reqs.py ← setuptools
backend/venv/lib/python3.14/site-packages/setuptools/archive_util.py ← distutils, _path, zipfile, tarfile, posixpath
backend/venv/lib/python3.14/site-packages/setuptools/build_meta.py ← ., _path, _reqs, warnings, distutils
backend/venv/lib/python3.14/site-packages/setuptools/depends.py ← ., _imp, extern, marshal, dis
backend/venv/lib/python3.14/site-packages/setuptools/discovery.py ← _path, distutils, _distutils_hack
backend/venv/lib/python3.14/site-packages/setuptools/dist.py ← distutils, extern, ., _importlib, config
backend/venv/lib/python3.14/site-packages/setuptools/errors.py ← distutils
backend/venv/lib/python3.14/site-packages/setuptools/extension.py ← monkey, distutils
backend/venv/lib/python3.14/site-packages/setuptools/installer.py ← distutils, ., wheel, warnings
backend/venv/lib/python3.14/site-packages/setuptools/launch.py ← tokenize
backend/venv/lib/python3.14/site-packages/setuptools/logging.py ← ., distutils
backend/venv/lib/python3.14/site-packages/setuptools/monkey.py ← importlib, platform, types, distutils
backend/venv/lib/python3.14/site-packages/setuptools/msvc.py ← setuptools, platform, distutils
backend/venv/lib/python3.14/site-packages/setuptools/namespaces.py ← distutils, compat
backend/venv/lib/python3.14/site-packages/setuptools/package_index.py ← pkg_resources, distutils, setuptools, configparser
backend/venv/lib/python3.14/site-packages/setuptools/sandbox.py ← distutils, pkg_resources, pickle
backend/venv/lib/python3.14/site-packages/setuptools/wheel.py ← distutils, setuptools, posixpath, zipfile
backend/venv/lib/python3.14/site-packages/setuptools/windows_support.py ← platform
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/_collections.py ← __future__
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/_macos_compat.py ← importlib
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/_modified.py ← _functools, errors, py39compat
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/_msvccompiler.py ← _log, ccompiler, errors, util, unittest
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/archive_util.py ← _log, dir_util, errors, spawn
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/bcppcompiler.py ← _log, _modified, ccompiler, errors, file_util
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/ccompiler.py ← _itertools, _log, _modified, dir_util, errors
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/cmd.py ← ., _log, errors
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/config.py ← configparser, cmd
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/core.py ← cmd, config, debug, dist, errors
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/cygwinccompiler.py ← _collections, errors, file_util, unixccompiler, version
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/dir_util.py ← _log, errors, errno
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/dist.py ← _log, debug, errors, fancy_getopt, util
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/fancy_getopt.py ← errors, getopt
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/file_util.py ← _log, errors
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/filelist.py ← _log, errors, util
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/log.py ← _log
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/msvc9compiler.py ← _log, ccompiler, errors, util, winreg
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/msvccompiler.py ← _log, ccompiler, errors
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/py39compat.py ← platform
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/spawn.py ← _log, debug, errors
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/sysconfig.py ← ., _functools, errors, sysconfig
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/unixccompiler.py ← __future__, ., compat, _log, _macos_compat
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/util.py ← _log, _modified, errors, spawn, distutils
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/versionpredicate.py ← .
backend/venv/lib/python3.14/site-packages/setuptools/_distutils/zosccompiler.py ← ., errors, unixccompiler
backend/venv/lib/python3.14/site-packages/setuptools/_vendor/zipp.py ← posixpath, zipfile
backend/venv/lib/python3.14/site-packages/setuptools/command/_requirestxt.py ← .., extern
backend/venv/lib/python3.14/site-packages/setuptools/command/alias.py ← distutils, setuptools
backend/venv/lib/python3.14/site-packages/setuptools/command/bdist_egg.py ← distutils, types, setuptools, _path, sysconfig
backend/venv/lib/python3.14/site-packages/setuptools/command/bdist_rpm.py ← warnings, distutils
backend/venv/lib/python3.14/site-packages/setuptools/command/build.py ← distutils, warnings
backend/venv/lib/python3.14/site-packages/setuptools/command/build_clib.py ← distutils
backend/venv/lib/python3.14/site-packages/setuptools/command/build_ext.py ← importlib, distutils, setuptools
backend/venv/lib/python3.14/site-packages/setuptools/command/build_py.py ← distutils, extern, warnings
backend/venv/lib/python3.14/site-packages/setuptools/command/develop.py ← distutils, setuptools
backend/venv/lib/python3.14/site-packages/setuptools/command/dist_info.py ← distutils, .., egg_info
backend/venv/lib/python3.14/site-packages/setuptools/command/easy_install.py ← distutils, sysconfig, setuptools, pkg_resources, compat
backend/venv/lib/python3.14/site-packages/setuptools/command/editable_wheel.py ← .., _path, compat, discovery, dist
backend/venv/lib/python3.14/site-packages/setuptools/command/egg_info.py ← distutils, _importlib, .., ., setuptools
backend/venv/lib/python3.14/site-packages/setuptools/command/install.py ← distutils, warnings, bdist_egg, platform, setuptools
backend/venv/lib/python3.14/site-packages/setuptools/command/install_egg_info.py ← distutils, setuptools, _path
backend/venv/lib/python3.14/site-packages/setuptools/command/install_lib.py ← distutils
backend/venv/lib/python3.14/site-packages/setuptools/command/install_scripts.py ← distutils, _path
backend/venv/lib/python3.14/site-packages/setuptools/command/register.py ← distutils, setuptools
backend/venv/lib/python3.14/site-packages/setuptools/command/rotate.py ← distutils, setuptools
backend/venv/lib/python3.14/site-packages/setuptools/command/saveopts.py ← setuptools
backend/venv/lib/python3.14/site-packages/setuptools/command/sdist.py ← distutils, _importlib, build
backend/venv/lib/python3.14/site-packages/setuptools/command/setopt.py ← distutils, setuptools, configparser
backend/venv/lib/python3.14/site-packages/setuptools/command/test.py ← distutils, unittest, pkg_resources, _importlib, setuptools
backend/venv/lib/python3.14/site-packages/setuptools/command/upload.py ← distutils, setuptools
backend/venv/lib/python3.14/site-packages/setuptools/command/upload_docs.py ← distutils, _importlib, warnings, upload, zipfile
backend/venv/lib/python3.14/site-packages/setuptools/config/_apply_pyprojecttoml.py ← types, _path, errors, warnings
backend/venv/lib/python3.14/site-packages/setuptools/config/expand.py ← configparser, importlib, types, distutils, _path
backend/venv/lib/python3.14/site-packages/setuptools/config/pyprojecttoml.py ← _path, errors, warnings, ., _apply_pyprojecttoml
backend/venv/lib/python3.14/site-packages/setuptools/config/setupcfg.py ← _path, errors, extern, warnings, .
backend/venv/lib/python3.14/site-packages/setuptools/extern/__init__.py ← importlib
backend/delete_enrollments_temp.py ← services
backend/reset_db_temp.py ← services
backend/seed_real_faces.py ← PIL, services, numpy, face_recognition
backend/download_test_images.py ← services
mobile/services/api.js ← store/useStore
backend/insert_seed_users.py ← services
backend/main.py ← fastapi, routes
backend/routes/auth.py ← fastapi, pydantic, services, jose
backend/routes/outpass.py ← fastapi, services, routes
backend/routes/face.py ← fastapi, services, routes, pydantic
backend/routes/students.py ← fastapi, services, routes, pydantic
backend/services/face_engine.py ← PIL, face_recognition, numpy
backend/test_api.py ← services, uvicorn
```

## todos
```
backend/venv/lib/python3.14/site-packages/cryptography/x509/name.py:368  # TODO: this is relatively expensive, if this looks like a bottleneck
backend/venv/lib/python3.14/site-packages/realtime/message.py:56  # TODO: what goes in here?
backend/venv/lib/python3.14/site-packages/realtime/types.py:102  # TODO: improve this
backend/venv/lib/python3.14/site-packages/fastapi/_compat/shared.py:179  # TODO: remove this function once the required version of Pydantic fully
backend/venv/lib/python3.14/site-packages/fastapi/_compat/shared.py:191  # TODO: remove this function once the required version of Pydantic fully
backend/venv/lib/python3.14/site-packages/fastapi/_compat/v2.py:58  # TODO: remove when this is merged (or equivalent): https://github.com/pydanti
backend/venv/lib/python3.14/site-packages/fastapi/_compat/v2.py:73  # TODO: remove when dropping support for Pydantic < v2.12.3
backend/venv/lib/python3.14/site-packages/fastapi/_compat/v2.py:99  # TODO: remove when dropping support for Pydantic < v2.12.3
backend/venv/lib/python3.14/site-packages/fastapi/_compat/v2.py:153  # TODO: remove after setting the min Pydantic to v2.12.3
backend/venv/lib/python3.14/site-packages/fastapi/_compat/v2.py:277  # TODO: remove when deprecating Pydantic v1
backend/venv/lib/python3.14/site-packages/fastapi/applications.py:929  # TODO: remove when discarding the openapi_prefix parameter
backend/venv/lib/python3.14/site-packages/fastapi/dependencies/utils.py:607  # TODO: remove this parameter later, no longer used, not removing it yet as so
backend/venv/lib/python3.14/site-packages/fastapi/encoders.py:58  # TODO: pv2 should this return strings instead?
backend/venv/lib/python3.14/site-packages/fastapi/openapi/utils.py:346  # TODO: probably make status_code a default class attribute for all
backend/venv/lib/python3.14/site-packages/fastapi/routing.py:1212  # TODO: Replace or deprecate this no-scope hook so included-route
backend/venv/lib/python3.14/site-packages/fastapi/routing.py:2167  # TODO: probably move this out of the Route / Route Group, same in APIRoute
backend/venv/lib/python3.14/site-packages/fastapi/routing.py:2501  # TODO: deprecate this once the lifespan (or alternative) interface is improve
backend/venv/lib/python3.14/site-packages/fastapi/routing.py:6312  # TODO: remove this once the lifespan (or alternative) interface is improved
backend/venv/lib/python3.14/site-packages/fastapi/routing.py:6328  # TODO: remove this once the lifespan (or alternative) interface is improved
backend/venv/lib/python3.14/site-packages/fastapi/routing.py:6344  # TODO: remove this once the lifespan (or alternative) interface is improved
```

## changes (last 10 commits — 21 hours ago)
```
backend/insert_seed_users.py                  +seed_users
backend/routes/outpass.py                     ~get_logs  ~currently_out
backend/routes/face.py                        ~enroll_face  ~log_action
```

## backend

### backend/venv/lib/python3.14/site-packages/httpx/_status_codes.py
```
class codes(IntEnum)
  def get_reason_phrase(value: int) → str
  def is_informational(value: int) → bool
  def is_success(value: int) → bool
  def is_redirect(value: int) → bool
  def is_client_error(value: int) → bool
  def is_server_error(value: int) → bool
  def is_error(value: int) → bool
```

### backend/venv/lib/python3.14/site-packages/httpx/_types.py
```
class SyncByteStream
  def close() → None
class AsyncByteStream
  async def aclose() → None
```

### backend/venv/lib/python3.14/site-packages/httpx/_urlparse.py
```
class ParseResult(typing.NamedTuple)
  def authority() → str
  def netloc() → str
  def copy_with(**kwargs: str | None) → ParseResult
def urlparse(url: str, **kwargs: str | None) → ParseResult
def encode_host(host: str) → str
def normalize_port(port: str | int | None, scheme: str) → int | None
def validate_path(path: str, has_scheme: bool, has_authority: bool) → None  # Path validation rules that depend on if the URL contains
def normalize_path(path: str) → str  # Drop "
def PERCENT(string: str) → str
def percent_encoded(string: str, safe: str) → str  # Use percent-encoding to quote a string
def quote(string: str, safe: str) → str  # Use percent-encoding to quote a string, omitting existing '%
```

### backend/venv/lib/python3.14/site-packages/httpx/_urls.py
```
class URL
  def __init__(url: URL | str, **kwargs: typing.Any) → None
  def scheme() → str
  def raw_scheme() → bytes
  def userinfo() → bytes
  def username() → str
  def password() → str
  def host() → str
  def raw_host() → bytes
class QueryParams(typing.Mapping[str, str])
  def __init__(*args: QueryParamTypes | None, **kwargs: typing.Any) → None
  def keys() → typing.KeysView[str]
  def values() → typing.ValuesView[str]
  def items() → typing.ItemsView[str, str]
  def multi_items() → list[tuple[str, str]]
  def get(key: typing.Any, default: typing.Any) → typing.Any
  def get_list(key: str) → list[str]
  def set(key: str, value: typing.Any) → QueryParams
```

### backend/venv/lib/python3.14/site-packages/httpx/_utils.py
```
class URLPattern
  def __init__(pattern: str) → None
  def matches(other: URL) → bool
  def priority() → tuple[int, int, int]
def primitive_value_to_str(value: PrimitiveData) → str  # Coerce a primitive data type into a string value
def get_environment_proxies() → dict[str, str | None]  # Gets proxy information from the environment
def to_bytes(value: str | bytes, encoding: str) → bytes
def to_str(value: str | bytes, encoding: str) → str
def to_bytes_or_str(value: str, match_type_of: typing.AnyStr) → typing.AnyStr
def unquote(value: str) → str
def peek_filelike_length(stream: typing.Any) → int | None  # Given a file-like stream object, return its length in number
def is_ipv4_hostname(hostname: str) → bool
def is_ipv6_hostname(hostname: str) → bool
```

### backend/venv/lib/python3.14/site-packages/httpx/_transports/asgi.py
```
class ASGIResponseStream(AsyncByteStream)
  def __init__(body: list[bytes]) → None
class ASGITransport(AsyncBaseTransport)
  async def receive() → dict[str, typing.Any]
  async def send(message: typing.MutableMapping[str, typing.Any]) → None
def is_running_trio() → bool
def create_event() → Event
```

### backend/venv/lib/python3.14/site-packages/httpx/_transports/base.py
```
class BaseTransport
  def handle_request(request: Request) → Response
  def close() → None
class AsyncBaseTransport
  async def aclose() → None
```

### backend/venv/lib/python3.14/site-packages/httpx/_transports/default.py
```
class ResponseStream(SyncByteStream)
  def __init__(httpcore_stream: typing.Iterable[bytes]) → None
  def close() → None
class HTTPTransport(BaseTransport)
  def close() → None
class AsyncResponseStream(AsyncByteStream)
  def __init__(httpcore_stream: typing.AsyncIterable[bytes]) → None
  async def aclose() → None
class AsyncHTTPTransport(AsyncBaseTransport)
  async def aclose() → None
def map_httpcore_exceptions() → typing.Iterator[None]
```

### backend/venv/lib/python3.14/site-packages/httpx/_transports/mock.py
```
class MockTransport(AsyncBaseTransport, BaseTransport)
  def __init__(handler: SyncHandler | AsyncHandler) → None
```

### backend/venv/lib/python3.14/site-packages/httpx/_transports/wsgi.py
```
class WSGIByteStream(SyncByteStream)
  def __init__(result: typing.Iterable[bytes]) → None
  def close() → None
class WSGITransport(BaseTransport)
  def handle_request(request: Request) → Response
```

### backend/venv/lib/python3.14/site-packages/cryptography/exceptions.py
```
class UnsupportedAlgorithm(Exception)
  def __init__(message: str, reason: _Reasons | None) → None
class AlreadyFinalized(Exception)
class AlreadyUpdated(Exception)
class NotYetFinalized(Exception)
class InvalidTag(Exception)
class InvalidSignature(Exception)
class InternalError(Exception)
class InvalidKey(Exception)
```

### backend/venv/lib/python3.14/site-packages/cryptography/fernet.py
```
class InvalidToken(Exception)
class Fernet
  def generate_key() → bytes
  def encrypt(data: bytes) → bytes
  def encrypt_at_time(data: bytes, current_time: int) → bytes
  def decrypt(token: bytes | str, ttl: int | None) → bytes
  def extract_timestamp(token: bytes | str) → int
class MultiFernet
  def __init__(fernets: Iterable[Fernet])
  def encrypt(msg: bytes) → bytes
  def encrypt_at_time(msg: bytes, current_time: int) → bytes
  def rotate(msg: bytes | str) → bytes
  def decrypt(msg: bytes | str, ttl: int | None) → bytes
  def extract_timestamp(msg: bytes | str) → int
```

### backend/venv/lib/python3.14/site-packages/cryptography/hazmat/_oid.py
```
class ExtensionOID
class OCSPExtensionOID
class CRLEntryExtensionOID
class NameOID
class SignatureAlgorithmOID
class HashAlgorithmOID
class PublicKeyAlgorithmOID
class ExtendedKeyUsageOID
class OtherNameFormOID
class AuthorityInformationAccessOID
class SubjectInformationAccessOID
class CertificatePoliciesOID
class AttributeOID
```

### backend/venv/lib/python3.14/site-packages/cryptography/utils.py
```
class CryptographyDeprecationWarning(UserWarning)
class InterfaceNotImplemented(Exception)
class _DeprecatedValue
  def __init__(value: object, message: str, warning_class)
class _ModuleWithDeprecations(types.ModuleType)
  def __init__(module: types.ModuleType)
class Enum(enum.Enum)
def int_to_bytes(integer: int, length: int | None) → bytes
def deprecated(value: object, module_name: str, message: str, warning_class: type[Warning], name: str | None) → _DeprecatedValue
def cached_property(func: Callable) → property
```

### backend/venv/lib/python3.14/site-packages/cryptography/x509/base.py
```
class AttributeNotFound(Exception)
  def __init__(msg: str, oid: ObjectIdentifier) → None
class Attribute
  def oid() → ObjectIdentifier
  def value() → bytes
class Attributes
  def get_attribute_for_oid(oid: ObjectIdentifier) → Attribute
class Version(utils.Enum)
class InvalidVersion(Exception)
  def __init__(msg: str, parsed_version: int) → None
class CertificateSigningRequestBuilder
  def subject_name(name: Name) → CertificateSigningRequestBuild
class CertificateBuilder
  def issuer_name(name: Name) → CertificateBuilder
  def subject_name(name: Name) → CertificateBuilder
  def serial_number(number: int) → CertificateBuilder
  def not_valid_before(time: datetime.datetime) → CertificateBuilder
  def not_valid_after(time: datetime.datetime) → CertificateBuilder
class CertificateRevocationListBuilder
class RevokedCertificateBuilder
  def serial_number(number: int) → RevokedCertificateBuilder
  def build(backend: typing.Any) → RevokedCertificate
def random_serial_number() → int
```

### backend/venv/lib/python3.14/site-packages/cryptography/x509/certificate_transparency.py
```
class LogEntryType(utils.Enum)
class Version(utils.Enum)
class SignatureAlgorithm(utils.Enum)
```

### backend/venv/lib/python3.14/site-packages/cryptography/x509/extensions.py
```
class DuplicateExtension(Exception)
  def __init__(msg: str, oid: ObjectIdentifier) → None
class ExtensionNotFound(Exception)
  def __init__(msg: str, oid: ObjectIdentifier) → None
class ExtensionType(metaclass=abc.ABCMeta)
  def public_bytes() → bytes
class Extensions
  def __init__(extensions: Iterable[Extension[ExtensionType]]) → None
class CRLNumber(ExtensionType)
  def __init__(crl_number: int) → None
  def crl_number() → int
  def public_bytes() → bytes
class AuthorityKeyIdentifier(ExtensionType)
  def key_identifier() → bytes | None
  def authority_cert_serial_number() → int | None
  def public_bytes() → bytes
class SubjectKeyIdentifier(ExtensionType)
  def __init__(digest: bytes) → None
  def digest() → bytes
  def key_identifier() → bytes
  def public_bytes() → bytes
class AuthorityInformationAccess(ExtensionType)
  def __init__(descriptions: Iterable[AccessDescription]) → None
  def public_bytes() → bytes
class SubjectInformationAccess(ExtensionType)
```

### backend/venv/lib/python3.14/site-packages/cryptography/x509/general_name.py
```
class UnsupportedGeneralNameType(Exception)
class GeneralName(metaclass=abc.ABCMeta)
  def value() → typing.Any
class RFC822Name(GeneralName)
  def __init__(value: str) → None
  def value() → str
class DNSName(GeneralName)
  def __init__(value: str) → None
  def value() → str
class UniformResourceIdentifier(GeneralName)
  def __init__(value: str) → None
  def value() → str
class DirectoryName(GeneralName)
  def __init__(value: Name) → None
  def value() → Name
class RegisteredID(GeneralName)
  def __init__(value: ObjectIdentifier) → None
  def value() → ObjectIdentifier
class IPAddress(GeneralName)
  def __init__(value: _IPAddressTypes) → None
  def value() → _IPAddressTypes
class OtherName(GeneralName)
  def __init__(type_id: ObjectIdentifier, value: bytes) → None
  def type_id() → ObjectIdentifier
  def value() → bytes
```

### backend/venv/lib/python3.14/site-packages/cryptography/x509/name.py
```
class _ASN1Type(utils.Enum)
class NameAttribute(typing.Generic[NameAttributeValueType])
  def oid() → ObjectIdentifier
  def value() → NameAttributeValueType
  def rfc4514_attribute_name() → str
class RelativeDistinguishedName
  def __init__(attributes: Iterable[NameAttribute[str | bytes]])
class Name
  def from_bytes(data: bytes) → Name
  def rdns() → list[RelativeDistinguishedName
  def public_bytes(backend: typing.Any) → bytes
class _RFC4514NameParser
  def __init__(data: str, attr_name_overrides: _NameOidMap) → None
  def parse() → Name
```

### backend/venv/lib/python3.14/site-packages/cryptography/x509/ocsp.py
```
class OCSPResponderEncoding(utils.Enum)
class OCSPResponseStatus(utils.Enum)
class OCSPCertStatus(utils.Enum)
class _SingleResponse
class OCSPRequestBuilder
  def build() → OCSPRequest
class OCSPResponseBuilder
```

### backend/venv/lib/python3.14/site-packages/realtime/_async/channel.py
```
class AsyncRealtimeChannel
  def on_join_push_ok(payload: ReplyPostgresChanges)
  def on_join_push_timeout()
  def on_close()
  def on_error(payload: dict[str, Any])
  def is_closed()
  def is_joining()
  def is_leaving()
  def is_errored()
```

### backend/venv/lib/python3.14/site-packages/realtime/_async/client.py
```
class AsyncRealtimeClient
  def is_connected() → bool
  async def connect() → None
  async def listen()
  async def close() → None
  def get_channels() → List[AsyncRealtimeChannel]
  async def remove_channel(channel: AsyncRealtimeChannel) → None
  async def remove_all_channels() → None
  async def set_auth(token: str?) → None
def deprecated(func: Callable) → Callable
```

### backend/venv/lib/python3.14/site-packages/realtime/_async/presence.py
```
class AsyncRealtimePresence
  def __init__()
  def on_join(callback: PresenceOnJoinCallback)
  def on_leave(callback: PresenceOnLeaveCallback)
  def on_sync(callback: Callable[[], None])
```

### backend/venv/lib/python3.14/site-packages/realtime/_async/push.py
```
class AsyncPush
  async def resend()
  async def send()
  def update_payload(payload: Dict[str, Any])
  def receive(status, callback) → AsyncPush
  def start_timeout()
  async def timeout()
  def trigger(status: RealtimeAcknowledgementStatus, response) → None
  def destroy()
```

### backend/venv/lib/python3.14/site-packages/realtime/_async/timer.py
```
class AsyncTimer
  def __init__(callback: Callable, timer_calc: Callable[[int], float])
  def reset()
  def schedule_timeout()
```

### backend/venv/lib/python3.14/site-packages/realtime/_sync/channel.py
```
class SyncRealtimeChannel
```

### backend/venv/lib/python3.14/site-packages/realtime/_sync/client.py
```
class SyncRealtimeClient
  def get_channels() → List[SyncRealtimeChannel]
  def remove_channel(channel: SyncRealtimeChannel) → None
  def remove_all_channels() → None
  def set_auth(token: str?) → None
```

### backend/venv/lib/python3.14/site-packages/realtime/_sync/presence.py
```
class SyncRealtimePresence
  def __init__(channel, opts: PresenceOpts?)
```

### backend/venv/lib/python3.14/site-packages/realtime/exceptions.py
```
class NotConnectedError(Exception)
  def __init__(func_name: str)
class AuthorizationError(Exception)
  def __init__(message: str?)
```

### backend/venv/lib/python3.14/site-packages/realtime/message.py
```
class Message(BaseModel) {event*, payload*, topic*, ref?, join_ref?}
class JoinMessage(BaseModel) {event*, topic*, ref*, payload*}
class PostgresRowChange(BaseModel) {id*, event*, table?, schema_?, filter?}
class ReplyPostgresChanges(BaseModel) {postgres_changes?}
class SuccessReplyMessage(BaseModel) {status*, response*}
class ErrorReplyMessage(BaseModel) {status*, response*}
class ReplyMessage(BaseModel) {event*, topic*, payload*, ref?}
class SuccessSystemPayload(BaseModel) {channel*, extension*, message*, status*}
class ErrorSystemPayload(BaseModel) {channel*, extension*, message*, status*}
class SystemMessage(BaseModel) {event*, topic*, payload*, ref*}
class HeartbeatPayload(BaseModel) {pass*}
class HeartbeatMessage(BaseModel) {event*, topic*, ref*, payload*}
class AccessTokenPayload(BaseModel) {access_token*}
class AccessTokenMessage(BaseModel) {event*, topic*, payload*, ref*}
class PostgresChangesMessage(BaseModel) {event*, topic*, payload*, ref*}
class BroadcastMessage(BaseModel) {event*, topic*, payload*, ref*}
class PresenceMessage(BaseModel) {event*, topic*, payload*, ref*}
class PresenceStateMessage(BaseModel) {event*, topic*, payload*, ref*}
class PresenceDiffMessage(BaseModel) {event*, topic*, payload*, ref*}
class ChannelErrorMessage(BaseModel) {event*, topic*, payload*, ref?}
class ChannelCloseMessage(BaseModel) {event*, topic*, payload*, ref?}
```

### backend/venv/lib/python3.14/site-packages/realtime/transformers.py
```
def http_endpoint_url(socket_url: str) → str
```

### backend/venv/lib/python3.14/site-packages/realtime/types.py
```
class ChannelEvents(str, Enum)
class ChannelStates(str, Enum)
class RealtimeSubscribeStates(str, Enum)
class RealtimePresenceListenEvents(str, Enum)
class RealtimeAcknowledgementStatus(str, Enum)
class RealtimePostgresChangesListenEvent(str, Enum)
class PostgresChangesColumn(TypedDict)
class PostgresChangesData(TypedDict)
class PostgresChangesPayload(TypedDict)
class BroadcastMeta(TypedDict, total=False)
class BroadcastPayload(TypedDict)
@dataclass BroadcastCallback(callback, event)
@dataclass PostgresChangesCallback(callback, event, table?, schema?, filter?, id?)
class _Hook
  def __init__(status: str, callback: Callback[[Dict[str, Any]], None])
class Presence(TypedDict)
class PresenceEvents
  def __init__(state: str, diff: str)
class PresenceOpts
  def __init__(events: PresenceEvents)
class ReplayOption(TypedDict, total=False)
class RealtimeChannelBroadcastConfig(TypedDict, total=False)
class RealtimeChannelPresenceConfig(TypedDict)
class RealtimeChannelConfig(TypedDict)
class RealtimeChannelOptions(TypedDict)
```

### backend/venv/lib/python3.14/site-packages/realtime/utils.py
```
def is_ws_url(url: str) → bool
```

### backend/venv/lib/python3.14/site-packages/realtime-2.31.0.dist-info/METADATA
```
def _on_subscribe(status: RealtimeSubscribeStates, err: Optional[Exception]):
```

### backend/venv/lib/python3.14/site-packages/fastapi-0.139.0.dist-info/METADATA
```
def read_root():
def read_item(item_id: int, q: str | None = None):
class Item(BaseModel):
def read_root():
def read_item(item_id: int, q: str | None = None):
def update_item(item_id: int, item: Item):
```

### backend/venv/lib/python3.14/site-packages/fastapi/_compat/shared.py
```
def lenient_issubclass(cls: Any, class_or_tuple: type[_T] | tuple[type[_T], ...] | None) → TypeGuard[type[_T]]
def field_annotation_is_sequence(annotation: type[Any] | None) → bool
def value_is_sequence(value: Any) → bool
def field_annotation_is_complex(annotation: type[Any] | None) → bool
def field_annotation_is_scalar(annotation: Any) → bool
def field_annotation_is_scalar_sequence(annotation: type[Any] | None) → bool
def is_bytes_or_nonable_bytes_annotation(annotation: Any) → bool
def is_uploadfile_or_nonable_uploadfile_annotation(annotation: Any) → bool
def is_bytes_sequence_annotation(annotation: Any) → bool
def is_uploadfile_sequence_annotation(annotation: Any) → bool
def is_pydantic_v1_model_instance(obj: Any) → bool
def is_pydantic_v1_model_class(cls: Any) → bool
def annotation_is_pydantic_v1(annotation: Any) → bool
```

### backend/venv/lib/python3.14/site-packages/fastapi/_compat/v2.py
```
class GenerateJsonSchema(_GenerateJsonSchema)
  def bytes_schema(schema: CoreSchema) → JsonSchemaValue
@dataclass ModelField(field_info, name, mode, config?)
def evaluate_forwardref(value: Any, globalns: dict[str, Any] | None, localns: dict[str, Any] | None) → Any
def asdict(field_info: FieldInfo) → dict[str, Any]
def get_schema_from_model_field(*, field: ModelField, model_name_map: ModelNameMap, field_mapping: dict[
        tuple[ModelField, Literal["validation", "serialization"]], JsonSchemaValue
    ], separate_input_output_schemas: bool) → dict[str, Any]
def get_definitions(*, fields: Sequence[ModelField], model_name_map: ModelNameMap, separate_input_output_schemas: bool) → tuple[
    dict[tuple[Model...
def is_scalar_field(field: ModelField) → bool
def copy_field_info(*, field_info: FieldInfo, annotation: Any) → FieldInfo
def serialize_sequence_value(*, field: ModelField, value: Any) → Sequence[Any]
def get_missing_field_error(loc: tuple[int | str, ...]) → dict[str, Any]
def create_body_model(*, fields: Sequence[ModelField], model_name: str) → type[BaseModel]
def get_model_fields(model: type[BaseModel]) → list[ModelField]
def get_cached_model_fields(model: type[BaseModel]) → list[ModelField]
def normalize_name(name: str) → str
def get_model_name_map(unique_models: TypeModelSet) → dict[TypeModelOrEnum, str]
def get_flat_models_from_model(model: type["BaseModel"], known_models: TypeModelSet | None) → TypeModelSet
def get_flat_models_from_annotation(annotation: Any, known_models: TypeModelSet) → TypeModelSet
def get_flat_models_from_field(field: ModelField, known_models: TypeModelSet) → TypeModelSet
def get_flat_models_from_fields(fields: Sequence[ModelField], known_models: TypeModelSet) → TypeModelSet
```

### backend/venv/lib/python3.14/site-packages/fastapi/applications.py
```
class FastAPI(Starlette)
  def build_middleware_stack() → ASGIApp
  def openapi() → dict[str, Any]
  def setup() → None
  async def openapi(req: Request) → JSONResponse
  async def swagger_ui_html(req: Request) → HTMLResponse
  async def swagger_ui_redirect(req: Request) → HTMLResponse
  async def redoc_html(req: Request) → HTMLResponse
  def decorator(func: DecoratedCallable) → DecoratedCallable
GET /items/  →  read_items()
GET /items/  →  read_items()
PUT /items/{item_id}  →  replace_item()
POST /items/  →  create_item()
DELETE /items/{item_id}  →  delete_item()
HEAD /items/  →  get_items_headers()
PATCH /items/  →  update_item()
```

### backend/venv/lib/python3.14/site-packages/fastapi/background.py
```
class BackgroundTasks(StarletteBackgroundTasks)
POST /send-notification/{email}  →  send_notification()
```

### backend/venv/lib/python3.14/site-packages/fastapi/cli.py
```
def main() → None
```

### backend/venv/lib/python3.14/site-packages/fastapi/concurrency.py
```
async def contextmanager_in_threadpool(cm: AbstractContextManager[_T]) → AsyncGenerator[_T, None]
```

### backend/venv/lib/python3.14/site-packages/fastapi/datastructures.py
```
class UploadFile(StarletteUploadFile)
  async def close() → None
class DefaultPlaceholder
  def __init__(value: Any)
def Default(value: DefaultType) → DefaultType  # You shouldn't use this function directly
POST /files/  →  create_file()
POST /uploadfile/  →  create_upload_file()
```

### backend/venv/lib/python3.14/site-packages/fastapi/dependencies/models.py
```
@dataclass Dependant(path_params, query_params, header_params, cookie_params, body_params, dependencies, name?, call?, request_param_name?, websocket_param_name?, http_connection_param_name?, response_param_name?, background_tasks_param_name?, security_scopes_param_name?, own_oauth_scopes?, parent_oauth_scopes?, use_cache, path?, scope?)
```

### backend/venv/lib/python3.14/site-packages/fastapi/dependencies/utils.py
```
@dataclass ParamDetails(type_annotation, depends, field)
@dataclass SolvedDependency(values, errors, background_tasks, response, dependency_cache)
def ensure_multipart_is_installed() → None
def get_parameterless_sub_dependant(*, depends: params.Depends, path: str) → Dependant
def get_flat_dependant(dependant: Dependant, *, skip_repeats: bool, visited: list[DependencyCacheKey] | None, parent_oauth_scopes: list[str] | None) → Dependant
def get_flat_params(dependant: Dependant) → list[ModelField]
def get_typed_signature(call: Callable[..., Any]) → inspect.Signature
def get_typed_annotation(annotation: Any, globalns: dict[str, Any]) → Any
def get_typed_return_annotation(call: Callable[..., Any]) → Any
def get_stream_item_type(annotation: Any) → Any | None
def get_dependant(*, path: str, call: Callable[..., Any], name: str | None, own_oauth_scopes: list[str] | None, parent_oauth_scopes: list[str] | None, use_cache: bool, scope: Literal["function", "request"] | None) → Dependant
def add_non_field_param_to_dependency(*, param_name: str, type_annotation: Any, dependant: Dependant) → bool | None
def analyze_param(*, param_name: str, annotation: Any, value: Any, is_path_param: bool) → ParamDetails
def add_param_to_fields(*, field: ModelField, dependant: Dependant) → None
async def solve_dependencies(*, request: Request | WebSocket, dependant: Dependant, body: dict[str, Any] | FormData | bytes | None, background_tasks: StarletteBackgroundTasks | None, response: Response | None, dependency_overrides_provider: Any | None, dependency_cache: dict[DependencyCacheKey, Any] | None, async_exit_stack: AsyncExitStack, embed_body_fields: bool) → SolvedDependency
def request_params_to_args(fields: Sequence[ModelField], received_params: Mapping[str, Any] | QueryParams | Headers) → tuple[dict[str, Any], list[...
def is_union_of_base_models(field_type: Any) → bool  # Check if field type is a Union where all members are BaseMod
async def request_body_to_args(body_fields: list[ModelField], received_body: dict[str, Any] | FormData | bytes | None, embed_body_fields: bool) → tuple[dict[str, Any], list[...
def get_body_field(*, flat_dependant: Dependant, name: str, embed_body_fields: bool) → ModelField | None  # Get a ModelField representing the request body for a path op
def get_validation_alias(field: ModelField) → str
```

### backend/venv/lib/python3.14/site-packages/fastapi/encoders.py
```
def isoformat(o: datetime.date | datetime.time) → str
def decimal_encoder(dec_value: Decimal) → int | float  # Encodes a Decimal as int if there's no exponent, otherwise f
def generate_encoders_by_class_tuples(type_encoder_map: dict[Any, Callable[[Any], Any]]) → dict[Callable[[Any], Any], ...
```

### backend/venv/lib/python3.14/site-packages/fastapi/exception_handlers.py
```
async def http_exception_handler(request: Request, exc: HTTPException) → Response
async def request_validation_exception_handler(request: Request, exc: RequestValidationError) → JSONResponse
async def websocket_request_validation_exception_handler(websocket: WebSocket, exc: WebSocketRequestValidationError) → None
```

### backend/venv/lib/python3.14/site-packages/fastapi/exceptions.py
```
class EndpointContext(TypedDict, total=False)
class HTTPException(StarletteHTTPException)
class WebSocketException(StarletteWebSocketException)
class FastAPIError(RuntimeError)
class DependencyScopeError(FastAPIError)
class ValidationException(Exception)
  def errors() → Sequence[Any]
class RequestValidationError(ValidationException)
class WebSocketRequestValidationError(ValidationException)
class ResponseValidationError(ValidationException)
class PydanticV1NotSupportedError(FastAPIError)
class FastAPIDeprecationWarning(UserWarning)
GET /items/{item_id}  →  read_item()
```

### backend/venv/lib/python3.14/site-packages/fastapi/middleware/asyncexitstack.py
```
class AsyncExitStackMiddleware
```

### backend/venv/lib/python3.14/site-packages/fastapi/openapi/docs.py
```
def get_swagger_ui_oauth2_redirect_html() → HTMLResponse  # Generate the HTML response with the OAuth2 redirection for S
```

### backend/venv/lib/python3.14/site-packages/fastapi/openapi/models.py
```
class BaseModelWithConfig(BaseModel) {model_config?}
class Contact(BaseModelWithConfig) {name?, url?, email?}
class License(BaseModelWithConfig) {name*, identifier?, url?}
class Info(BaseModelWithConfig) {title*, summary?, description?, termsOfService?, contact?, license?}
class ServerVariable(BaseModelWithConfig) {enum?, default*, description?}
class Server(BaseModelWithConfig) {url*, description?, variables?}
class Reference(BaseModel) {ref?}
class Discriminator(BaseModel) {propertyName*, mapping?}
class XML(BaseModelWithConfig) {name?, namespace?, prefix?, attribute?, wrapped?}
class ExternalDocumentation(BaseModelWithConfig) {description?, url*}
class Schema(BaseModelWithConfig) {schema_?, vocabulary?, id?, anchor?, dynamicAnchor?, ref?}
class Example(TypedDict, total=False)
class ParameterInType(Enum)
class Encoding(BaseModelWithConfig) {contentType?, headers?, style?, explode?, allowReserved?}
class MediaType(BaseModelWithConfig) {schema_?, example?, examples?, encoding?}
class ParameterBase(BaseModelWithConfig) {description?, required?, deprecated?, style?, explode?, allowReserved?}
class Parameter(ParameterBase)
class Header(ParameterBase)
class RequestBody(BaseModelWithConfig) {description?, content*, required?}
class Link(BaseModelWithConfig) {operationRef?, operationId?, parameters?, requestBody?, description?, server?}
class Response(BaseModelWithConfig) {description*, headers?, content?, links?}
class Operation(BaseModelWithConfig) {tags?, summary?, description?, externalDocs?, operationId?, parameters?}
class PathItem(BaseModelWithConfig) {ref?, summary?, description?, get?, put?, post?}
class SecuritySchemeType(Enum)
class SecurityBase(BaseModelWithConfig) {type_?, description?}
```

### backend/venv/lib/python3.14/site-packages/fastapi/openapi/utils.py
```
def get_openapi_security_definitions(flat_dependant: Dependant) → tuple[dict[str, Any], list[...
def get_openapi_operation_request_body(*, body_field: ModelField | None, model_name_map: ModelNameMap, field_mapping: dict[
        tuple[ModelField, Literal["validation", "serialization"]], dict[str, Any]
    ], separate_input_output_schemas: bool) → dict[str, Any] | None
def generate_operation_id(*, route: routing._APIRouteLike, method: str) → str
def generate_operation_summary(*, route: routing._APIRouteLike, method: str) → str
def get_openapi_operation_metadata(*, route: routing._APIRouteLike, method: str, operation_ids: set[str]) → dict[str, Any]
def get_openapi_path(*, route: routing._APIRouteLike, operation_ids: set[str], model_name_map: ModelNameMap, field_mapping: dict[
        tuple[ModelField, Literal["validation", "serialization"]], dict[str, Any]
    ], separate_input_output_schemas: bool) → tuple[dict[str, Any], dict[...
def get_fields_from_routes(routes: Sequence[BaseRoute | routing.RouteContext]) → list[ModelField]
def get_openapi(*, title: str, version: str, openapi_version: str, summary: str | None, description: str | None, routes: Sequence[BaseRoute | routing.RouteContext], webhooks: Sequence[BaseRoute | routing.RouteContext] | None, tags: list[dict[str, Any]] | None, servers: list[dict[str, str | Any]] | None, terms_of_service: str | None, contact: dict[str, str | Any] | None, license_info: dict[str, str | Any] | None, separate_input_output_schemas: bool, external_docs: dict[str, Any] | None) → dict[str, Any]
```

### backend/venv/lib/python3.14/site-packages/fastapi/param_functions.py
```
GET /items/{item_id}  →  read_items()
GET /items/  →  read_items()
GET /users/me/items/  →  read_own_items()
```

### backend/venv/lib/python3.14/site-packages/fastapi/params.py
```
class ParamTypes(Enum)
class Param(FieldInfo)
class Path(Param)
class Query(Param)
class Header(Param)
class Cookie(Param)
class Body(FieldInfo)
class Form(Body)
class File(Form)
@dataclass Depends(dependency?, use_cache, scope?)
@dataclass Security(scopes?)
```

### backend/venv/lib/python3.14/site-packages/fastapi/responses.py
```
class _UjsonModule(Protocol)
  def dumps(__obj: Any, *, ensure_ascii: bool) → str
class _OrjsonModule(Protocol)
  def dumps(__obj: Any, *, option: int) → bytes
class UJSONResponse(JSONResponse)
  def render(content: Any) → bytes
class ORJSONResponse(JSONResponse)
  def render(content: Any) → bytes
```

### backend/venv/lib/python3.14/site-packages/fastapi/routing.py
```
class _AsyncLiftContextManager(AbstractAsyncContextManager[_T])
  def __init__(cm: AbstractContextManager[_T]) → None
class _DefaultLifespan
  def __init__(router: "APIRouter") → None
class APIWebSocketRoute(routing.WebSocketRoute)
  def matches(scope: Scope) → tuple[Match, Scope]
class _RouteWithPath(Protocol)
class _APIRouteLike(Protocol)
class APIRoute(routing.Route)
  def get_route_handler() → Callable[[Request], Coroutine[
  def matches(scope: Scope) → tuple[Match, Scope]
  async def handle(scope: Scope, receive: Receive, send: Send) → None
@dataclass _RouterIncludeContext(included_router, prefix, tags, dependencies, default_response_class)
@dataclass _EffectiveRouteContext(original_route, starlette_route?, frontend_prefix, path, endpoint?, stream_item_type?, response_model?, summary?, response_description, deprecated?, operation_id?, response_model_include?, response_model_exclude?, response_model_by_alias, response_model_exclude_unset, response_model_exclude_defaults, response_model_exclude_none, include_in_schema, response_class)
@dataclass RouteContext(route, _route_context?)
@dataclass _IncludedRouter(original_router, include_context, _effective_candidates)
class _FrontendStaticFiles(StaticFiles)
  def get_path(scope: Scope) → str
  async def get_response(path: str, scope: Scope) → Response
class _FrontendRoute(BaseRoute)
  def matches(scope: Scope) → tuple[Match, Scope]
  def matches_with_path(scope: Scope, path: str) → tuple[Match, Scope]
  async def handle(scope: Scope, receive: Receive, send: Send) → None
  def url_path_for(name: str, /, **path_params: Any) → URLPath
class _FrontendRouteGroup(BaseRoute)
```

### backend/venv/lib/python3.14/site-packages/fastapi/security/api_key.py
```
class APIKeyBase(SecurityBase)
  def make_not_authenticated_error() → HTTPException
  def check_api_key(api_key: str | None) → str | None
class APIKeyQuery(APIKeyBase)
class APIKeyHeader(APIKeyBase)
class APIKeyCookie(APIKeyBase)
GET /items/  →  read_items()
GET /items/  →  read_items()
GET /items/  →  read_items()
```

### backend/venv/lib/python3.14/site-packages/fastapi/security/base.py
```
class SecurityBase
```

### backend/venv/lib/python3.14/site-packages/fastapi/security/http.py
```
class HTTPBasicCredentials(BaseModel) {username*, password*}
class HTTPAuthorizationCredentials(BaseModel) {scheme*, credentials*}
class HTTPBase(SecurityBase)
  def make_authenticate_headers() → dict[str, str]
  def make_not_authenticated_error() → HTTPException
class HTTPBasic(HTTPBase)
  def make_authenticate_headers() → dict[str, str]
class HTTPBearer(HTTPBase)
class HTTPDigest(HTTPBase)
GET /users/me  →  read_current_user()
GET /users/me  →  read_current_user()
GET /users/me  →  read_current_user()
```

### backend/venv/lib/python3.14/site-packages/fastapi/security/oauth2.py
```
class OAuth2PasswordRequestForm
class OAuth2PasswordRequestFormStrict(OAuth2PasswordRequestForm)
class OAuth2(SecurityBase)
  def make_not_authenticated_error() → HTTPException
class OAuth2PasswordBearer(OAuth2)
class OAuth2AuthorizationCodeBearer(OAuth2)
class SecurityScopes
POST /login  →  login()
POST /login  →  login()
```

### backend/venv/lib/python3.14/site-packages/fastapi/security/open_id_connect_url.py
```
class OpenIdConnect(SecurityBase)
  def make_not_authenticated_error() → HTTPException
```

### backend/venv/lib/python3.14/site-packages/fastapi/security/utils.py
```
def get_authorization_scheme_param(authorization_header_value: str | None) → tuple[str, str]
```

### backend/venv/lib/python3.14/site-packages/fastapi/sse.py
```
class EventSourceResponse(StreamingResponse)
class ServerSentEvent(BaseModel) {data*, raw_data*, event*, id*, retry*, comment*}
```

### backend/venv/lib/python3.14/site-packages/fastapi/utils.py
```
def is_body_allowed_for_status_code(status_code: int | str | None) → bool
def get_path_param_names(path: str) → set[str]
def create_model_field(name: str, type_: Any, default: Any | None, field_info: FieldInfo | None, alias: str | None, mode: Literal["validation", "serialization"]) → ModelField
def generate_operation_id_for_path(*, name: str, path: str, method: str) → str
def generate_unique_id(route: "APIRoute") → str
def deep_dict_update(main_dict: dict[Any, Any], update_dict: dict[Any, Any]) → None
def get_value_or_default(first_item: DefaultPlaceholder | DefaultType, *extra_items: DefaultPlaceholder | DefaultType) → DefaultPlaceholder | Defaul...  # Pass items or `DefaultPlaceholder`s by descending priority
```

### backend/venv/lib/python3.14/site-packages/supabase_functions/__init__.py
```
def create_client(url: str, headers: dict[str, str], *, is_async: Literal[True], verify: bool) → AsyncFunctionsClient
def create_client(url: str, headers: dict[str, str], *, is_async: Literal[False], verify: bool) → SyncFunctionsClient
def create_client(url: str, headers: dict[str, str], *, is_async: bool, verify: bool) → Union[AsyncFunctionsClient,...
```

### backend/venv/lib/python3.14/site-packages/supabase_functions/_async/functions_client.py
```
class AsyncFunctionsClient
  def set_auth(token: str) → None
```

### backend/venv/lib/python3.14/site-packages/supabase_functions/_sync/functions_client.py
```
class SyncFunctionsClient
  def set_auth(token: str) → None
```

### backend/venv/lib/python3.14/site-packages/supabase_functions/errors.py
```
class FunctionsApiErrorDict(TypedDict)
class FunctionsError(Exception)
  def __init__(message: str, name: str, status: int) → None
  def to_dict() → FunctionsApiErrorDict
class FunctionsHttpError(FunctionsError)
  def __init__(message: str, code: int | None) → None
class FunctionsRelayError(FunctionsError)
  def __init__(message: str, code: int | None) → None
```

### backend/venv/lib/python3.14/site-packages/supabase_functions/utils.py
```
class FunctionRegion(StrEnum)
def is_valid_str_arg(target: str) → bool
def is_http_url(url: str) → bool
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_admin_api.py
```
class AsyncGoTrueAdminAPI(AsyncGoTrueBaseAPI)
  async def sign_out(jwt: str, scope: SignOutScope) → None
  async def generate_link(params: GenerateLinkParams) → GenerateLinkResponse
  async def create_user(attributes: AdminUserAttributes) → UserResponse
  async def get_user_by_id(uid: str) → UserResponse
  async def delete_user(id: str, should_soft_delete: bool) → None
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_admin_mfa_api.py
```
class AsyncGoTrueAdminMFAAPI
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_admin_oauth_api.py
```
class AsyncGoTrueAdminOAuthAPI
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_base_api.py
```
class AsyncGoTrueBaseAPI
  async def close() → None
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_client.py
```
class AsyncGoTrueClient(AsyncGoTrueBaseAPI)
  async def initialize(*, url: str?) → None
  async def initialize_from_storage() → None
  async def initialize_from_url(url: str) → None
  async def get_user_identities() → IdentitiesResponse
  async def unlink_identity(identity: UserIdentity) → Response
  async def verify_otp(params: VerifyOtpParams) → AuthResponse
  async def reauthenticate() → AuthResponse
  async def get_session() → Session|None
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_async/gotrue_mfa_api.py
```
class AsyncGoTrueMFAAPI
  async def enroll(params: MFAEnrollParams) → AuthMFAEnrollResponse
  async def challenge(params: MFAChallengeParams) → AuthMFAChallengeResponse
  async def verify(params: MFAVerifyParams) → AuthMFAVerifyResponse
  async def unenroll(params: MFAUnenrollParams) → AuthMFAUnenrollResponse
  async def list_factors() → AuthMFAListFactorsResponse
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_async/storage.py
```
class AsyncSupportedStorage(ABC)
  async def get_item(key: str) → str|None
  async def set_item(key: str, value: str) → None
  async def remove_item(key: str) → None
class AsyncMemoryStorage(AsyncSupportedStorage)
  def __init__() → None
  async def get_item(key: str) → str|None
  async def set_item(key: str, value: str) → None
  async def remove_item(key: str) → None
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_admin_api.py
```
class SyncGoTrueAdminAPI(SyncGoTrueBaseAPI)
  def sign_out(jwt: str, scope: SignOutScope) → None
  def generate_link(params: GenerateLinkParams) → GenerateLinkResponse
  def create_user(attributes: AdminUserAttributes) → UserResponse
  def get_user_by_id(uid: str) → UserResponse
  def delete_user(id: str, should_soft_delete: bool) → None
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_admin_mfa_api.py
```
class SyncGoTrueAdminMFAAPI
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_admin_oauth_api.py
```
class SyncGoTrueAdminOAuthAPI
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_base_api.py
```
class SyncGoTrueBaseAPI
  def close() → None
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_client.py
```
class SyncGoTrueClient(SyncGoTrueBaseAPI)
  def initialize(*, url: str?) → None
  def initialize_from_storage() → None
  def initialize_from_url(url: str) → None
  def sign_in_with_sso(credentials: SignInWithSSOCredentials) → SSOResponse
  def link_identity(credentials: SignInWithOAuthCredentials) → OAuthResponse
  def get_user_identities() → IdentitiesResponse
  def unlink_identity(identity: UserIdentity) → Response
  def verify_otp(params: VerifyOtpParams) → AuthResponse
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/gotrue_mfa_api.py
```
class SyncGoTrueMFAAPI
  def enroll(params: MFAEnrollParams) → AuthMFAEnrollResponse
  def challenge(params: MFAChallengeParams) → AuthMFAChallengeResponse
  def verify(params: MFAVerifyParams) → AuthMFAVerifyResponse
  def unenroll(params: MFAUnenrollParams) → AuthMFAUnenrollResponse
  def list_factors() → AuthMFAListFactorsResponse
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/_sync/storage.py
```
class SyncSupportedStorage(ABC)
  def get_item(key: str) → str|None
  def set_item(key: str, value: str) → None
  def remove_item(key: str) → None
class SyncMemoryStorage(SyncSupportedStorage)
  def __init__() → None
  def get_item(key: str) → str|None
  def set_item(key: str, value: str) → None
  def remove_item(key: str) → None
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/errors.py
```
class UserDoesntExist(Exception)
  def __init__(access_token: str) → None
class AuthError(Exception)
  def __init__(message: str, code: ErrorCode | None) → None
class AuthApiErrorDict(TypedDict)
class AuthApiError(AuthError)
  def __init__(message: str, status: int, code: ErrorCode?) → None
  def to_dict() → AuthApiErrorDict
class AuthUnknownError(AuthError)
  def __init__(message: str, original_error: Exception) → None
class CustomAuthError(AuthError)
  def to_dict() → AuthApiErrorDict
class AuthSessionMissingError(CustomAuthError)
  def __init__() → None
class AuthInvalidCredentialsError(CustomAuthError)
  def __init__(message: str) → None
class AuthImplicitGrantRedirectErrorDetails(TypedDict)
class AuthImplicitGrantRedirectErrorDict(AuthApiErrorDict)
class AuthImplicitGrantRedirectError(CustomAuthError)
  def to_dict() → AuthImplicitGrantRedirectError
class AuthRetryableError(CustomAuthError)
  def __init__(message: str, status: int) → None
class AuthApiErrorWithReasonsDict(AuthApiErrorDict)
class AuthWeakPasswordError(CustomAuthError)
  def __init__(message: str, status: int, reasons: List[str]) → None
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/helpers.py
```
class DecodedJWT(TypedDict)
def model_validate(model: Type[TBaseModel], contents: Union[str, bytes]) → TBaseModel  # Compatibility layer between pydantic 1 and 2 for parsing an 
def model_dump(model: BaseModel) → Dict[str, Any]  # Compatibility layer between pydantic 1 and 2 for dumping a m
def model_dump_json(model: BaseModel) → str  # Compatibility layer between pydantic 1 and 2 for dumping a m
def parse_auth_response(response: Response) → AuthResponse
def parse_auth_otp_response(response: Response) → AuthOtpResponse
def parse_link_identity_response(response: Response) → LinkIdentityResponse
def parse_link_response(response: Response) → GenerateLinkResponse
def parse_user_response(response: Response) → UserResponse
def parse_sso_response(response: Response) → SSOResponse
def parse_jwks(response: Response) → JWKSet
def get_error_message(error: Any) → str
def handle_exception(error: HTTPStatusError | RuntimeError) → AuthError
def str_from_base64url(base64url: str) → str
def base64url_to_bytes(base64url: str) → bytes
def decode_jwt(token: str) → DecodedJWT
def generate_pkce_verifier(length) → str  # Generate a random PKCE verifier of the specified length
def generate_pkce_challenge(code_verifier) → str  # Generate a code challenge from a PKCE verifier
def parse_response_api_version(response: Response) → datetime|None
def is_http_url(url: str) → bool
def validate_exp(exp: int) → None
def is_valid_uuid(value: str) → bool
def validate_uuid(id: str | None) → None
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/timer.py
```
class Timer
  def start() → None
  async def schedule() → None
  def cleanup(_) → None
  def cancel() → None
  def is_alive() → bool
```

### backend/venv/lib/python3.14/site-packages/supabase_auth/types.py
```
class AMREntry(BaseModel) {method*, timestamp*}
class AMREntryDict(TypedDict)
class Options(TypedDict)
class UpdateUserOptions(TypedDict)
class InviteUserByEmailOptions(TypedDict)
class AuthResponse(BaseModel) {user?, session?}
class AuthOtpResponse(BaseModel) {user?, session?, message_id?}
class OAuthResponse(BaseModel) {provider*, url*}
class SSOResponse(BaseModel) {url*}
class LinkIdentityResponse(BaseModel) {url*}
class IdentitiesResponse(BaseModel) {identities*}
class UserList(BaseModel) {users*}
class UserResponse(BaseModel) {user*}
class Session(BaseModel) {provider_token?, provider_refresh_token?, access_token*, refresh_token*, expires_in*, expires_at?}
class UserIdentity(BaseModel) {id*, identity_id*, user_id*, identity_data*, provider*, created_at*}
class Factor(BaseModel) {id*, friendly_name?, factor_type*, status*, created_at*, updated_at*}
class User(BaseModel) {id*, app_metadata*, user_metadata*, aud*, confirmation_sent_at?, recovery_sent_at?}
class UserAttributes(TypedDict)
class AdminUserAttributes(UserAttributes, TypedDict)
class Subscription(BaseModel) {id*, callback?, unsubscribe*}
class UpdatableFactorAttributes(TypedDict)
class SignUpWithEmailAndPasswordCredentialsOptions(TypedDict,)
class SignUpWithEmailAndPasswordCredentials(TypedDict)
class SignUpWithPhoneAndPasswordCredentialsOptions(TypedDict)
class SignUpWithPhoneAndPasswordCredentials(TypedDict)
```

### backend/venv/lib/python3.14/site-packages/storage3/__init__.py
```
def create_client(url: str, headers: dict[str, str], *, is_async: Literal[True]) → AsyncStorageClient
def create_client(url: str, headers: dict[str, str], *, is_async: Literal[False]) → SyncStorageClient
def create_client(url: str, headers: dict[str, str], *, is_async: bool, timeout: int) → Union[AsyncStorageClient, S...
```

### backend/venv/lib/python3.14/site-packages/storage3/_async/analytics.py
```
class AsyncStorageAnalyticsClient
  def __init__(request: AsyncRequestBuilder) → None
  async def create(bucket_name: str) → AnalyticsBucket
  async def delete(bucket_name: str) → AnalyticsBucketDeleteResponse
```

### backend/venv/lib/python3.14/site-packages/storage3/_async/bucket.py
```
class AsyncStorageBucketAPI
  def __init__(session: AsyncClient, url: str, headers: Headers) → None
  async def list_buckets() → list[AsyncBucket]
  async def get_bucket(id: str) → AsyncBucket
  async def empty_bucket(id: str) → dict[str, str]
  async def delete_bucket(id: str) → dict[str, str]
```

### backend/venv/lib/python3.14/site-packages/storage3/_async/client.py
```
class AsyncStorageClient(AsyncStorageBucketAPI)
  def from_(id: str) → AsyncBucketProxy
  def vectors() → AsyncStorageVectorsClient
  def analytics() → AsyncStorageAnalyticsClient
```

### backend/venv/lib/python3.14/site-packages/storage3/_async/file_api.py
```
class AsyncBucketActionsMixin
  async def move(from_path: str, to_path: str) → dict[str, str]
  async def copy(from_path: str, to_path: str) → dict[str, str]
  async def remove(paths: list[str]) → list[dict[str, Any]]
class AsyncBucket(BaseBucket)
@dataclass AsyncBucketProxy(id, _base_url, _headers, _client)
def relative_path_to_parts(path: str) → tuple[str, ...]
```

### backend/venv/lib/python3.14/site-packages/storage3/_async/request.py
```
class AsyncRequestBuilder
  def __init__(session: AsyncClient, base_url: URL, headers: Headers) → None
```

### backend/venv/lib/python3.14/site-packages/storage3/_async/vectors.py
```
class AsyncVectorBucketScope
  def __init__(request: AsyncRequestBuilder, bucket_name: str) → None
  def with_metadata(**data: JSON) → JSON
  async def get_index(index_name: str) → GetVectorIndexResponse|None
  async def delete_index(index_name: str) → None
  def index(index_name: str) → AsyncVectorIndexScope
class AsyncVectorIndexScope
  def with_metadata(**data: JSON) → JSON
  async def put(vectors: List[VectorObject]) → None
  async def delete(keys: List[str]) → None
class AsyncStorageVectorsClient
  def __init__(url: URL, headers: Headers, session: AsyncClient) → None
  def from_(bucket_name: str) → AsyncVectorBucketScope
  async def create_bucket(bucket_name: str) → None
  async def get_bucket(bucket_name: str) → GetVectorBucketResponse|None
  async def delete_bucket(bucket_name: str) → None
def remove_none(**kwargs: JSON) → JSON
```

### backend/venv/lib/python3.14/site-packages/storage3/_sync/analytics.py
```
class SyncStorageAnalyticsClient
  def __init__(request: SyncRequestBuilder) → None
  def create(bucket_name: str) → AnalyticsBucket
  def delete(bucket_name: str) → AnalyticsBucketDeleteResponse
```

### backend/venv/lib/python3.14/site-packages/storage3/_sync/bucket.py
```
class SyncStorageBucketAPI
  def __init__(session: Client, url: str, headers: Headers) → None
  def list_buckets() → list[SyncBucket]
  def get_bucket(id: str) → SyncBucket
  def empty_bucket(id: str) → dict[str, str]
  def delete_bucket(id: str) → dict[str, str]
```

### backend/venv/lib/python3.14/site-packages/storage3/_sync/client.py
```
class SyncStorageClient(SyncStorageBucketAPI)
  def from_(id: str) → SyncBucketProxy
  def vectors() → SyncStorageVectorsClient
  def analytics() → SyncStorageAnalyticsClient
```

### backend/venv/lib/python3.14/site-packages/storage3/_sync/file_api.py
```
class SyncBucketActionsMixin
  def get_public_url(path: str, options: URLOptions?) → str
  def move(from_path: str, to_path: str) → dict[str, str]
  def copy(from_path: str, to_path: str) → dict[str, str]
  def remove(paths: list[str]) → list[dict[str, Any]]
class SyncBucket(BaseBucket)
@dataclass SyncBucketProxy(id, _base_url, _headers, _client)
def relative_path_to_parts(path: str) → tuple[str, ...]
```

### backend/venv/lib/python3.14/site-packages/storage3/_sync/request.py
```
class SyncRequestBuilder
  def __init__(session: Client, base_url: URL, headers: Headers) → None
```

### backend/venv/lib/python3.14/site-packages/storage3/_sync/vectors.py
```
class SyncVectorBucketScope
  def __init__(request: SyncRequestBuilder, bucket_name: str) → None
  def with_metadata(**data: JSON) → JSON
  def get_index(index_name: str) → GetVectorIndexResponse|None
  def delete_index(index_name: str) → None
  def index(index_name: str) → SyncVectorIndexScope
class SyncVectorIndexScope
  def with_metadata(**data: JSON) → JSON
  def put(vectors: List[VectorObject]) → None
  def delete(keys: List[str]) → None
class SyncStorageVectorsClient
  def __init__(url: URL, headers: Headers, session: Client) → None
  def from_(bucket_name: str) → SyncVectorBucketScope
  def create_bucket(bucket_name: str) → None
  def get_bucket(bucket_name: str) → GetVectorBucketResponse|None
  def delete_bucket(bucket_name: str) → None
def remove_none(**kwargs: JSON) → JSON
```

### backend/venv/lib/python3.14/site-packages/storage3/exceptions.py
```
class VectorBucketException(Exception)
  def __init__(msg: str) → None
class VectorBucketErrorMessage(BaseModel) {statusCode*, error*, message*, code?}
class StorageApiErrorDict(TypedDict)
class StorageApiError(StorageException)
  def __init__(message: str, code: str, status: Union[int, str]) → None
  def to_dict() → StorageApiErrorDict
```

### backend/venv/lib/python3.14/site-packages/storage3/types.py
```
class BaseBucket(BaseModel, extra="ignore") {id*, name*, owner*, public*, created_at*, updated_at*}
class _sortByType(TypedDict, total=False)
class SignedUploadURL(TypedDict)
class CreateOrUpdateBucketOptions(TypedDict, total=False)
class ListBucketFilesOptions(TypedDict, total=False)
class TransformOptions(TypedDict, total=False)
class URLOptions(TypedDict, total=False)
class CreateSignedURLsOptions(TypedDict, total=False)
class SortByV2(TypedDict, total=False)
class SearchV2Options(TypedDict, total=False)
class SearchV2Object(BaseModel) {id*, name*, updated_at*, created_at*, metadata*, key?}
class SearchV2Folder(BaseModel) {key*, name*, created_at?, updated_at?}
class SearchV2Result(BaseModel) {hasNext*, folders*, objects*, nextCursor?}
class DownloadOptions(TypedDict, total=False)
class UploadData(TypedDict, total=False)
@dataclass UploadResponse(path, full_path, fullPath)
class SignedUrlResponse(TypedDict)
class CreateSignedUrlResponse(TypedDict)
class SignedUrlJsonResponse(BaseModel, extra="ignore") {signedURL*}
class SignedUrlsJsonItem(BaseModel, extra="ignore") {error?, path*, signedURL?}
class CreateSignedUploadUrlOptions(BaseModel, extra="ignore") {upsert*}
class MetadataConfiguration(BaseModel, extra="ignore") {non_filterable_metadata_keys?}
class ListIndexesOptions(BaseModel, extra="ignore") {nextToken?, maxResults?, prefix?}
class ListIndexesResponseItem(BaseModel, extra="ignore") {indexName*}
class ListVectorIndexesResponse(BaseModel, extra="ignore") {indexes*, nextToken?}
```

### backend/venv/lib/python3.14/site-packages/storage3/utils.py
```
class SyncClient(Client)
  def __init__(*args, **kwargs) → None
  def aclose() → None
class StorageException(Exception)
```

### backend/venv/lib/python3.14/site-packages/postgrest/_async/client.py
```
class AsyncPostgrestClient(BasePostgrestClient)
  def schema(schema: str) → AsyncPostgrestClient
  async def aclose() → None
  def from_(table: str) → AsyncRequestBuilder
  def table(table: str) → AsyncRequestBuilder
  def from_table(table: str) → AsyncRequestBuilder
```

### backend/venv/lib/python3.14/site-packages/postgrest/_async/request_builder.py
```
class AsyncQueryRequestBuilder
  def __init__(request: ReqConfig)
  def select(: QueryBuilderT, *columns: str) → QueryBuilderT
  def retry(enabled: bool) → Self
  async def execute() → APIResponse
class AsyncSingleRequestBuilder
  def __init__(request: ReqConfig)
  def retry(enabled: bool) → Self
  async def execute() → SingleAPIResponse
class AsyncExplainRequestBuilder
  def __init__(request: ReqConfig)
  def retry(enabled: bool) → Self
  async def execute() → str
class AsyncMaybeSingleRequestBuilder
  def __init__(request: ReqConfig)
  def retry(enabled: bool) → Self
  async def execute() → SingleAPIResponse|None
class AsyncFilterRequestBuilder(BaseFilterRequestBuilder[AsyncClient], AsyncQueryRequestBuilder)
  def __init__(request: ReqConfig) → None
class AsyncRPCFilterRequestBuilder(BaseRPCRequestBuilder, AsyncSingleRequestBuilder)
  def __init__(request: ReqConfig) → None
class AsyncSelectRequestBuilder(AsyncQueryRequestBuilder, BaseSelectRequestBuilder[AsyncClient])
  def __init__(request: ReqConfig) → None
  def single() → AsyncSingleRequestBuilder
  def maybe_single() → AsyncMaybeSingleRequestBuilder
```

### backend/venv/lib/python3.14/site-packages/postgrest/_sync/client.py
```
class SyncPostgrestClient(BasePostgrestClient)
  def schema(schema: str) → SyncPostgrestClient
  def aclose() → None
  def from_(table: str) → SyncRequestBuilder
  def table(table: str) → SyncRequestBuilder
  def from_table(table: str) → SyncRequestBuilder
```

### backend/venv/lib/python3.14/site-packages/postgrest/_sync/request_builder.py
```
class SyncQueryRequestBuilder
  def __init__(request: ReqConfig)
  def select(: QueryBuilderT, *columns: str) → QueryBuilderT
  def retry(enabled: bool) → Self
  def execute() → APIResponse
class SyncSingleRequestBuilder
  def __init__(request: ReqConfig)
  def retry(enabled: bool) → Self
  def execute() → SingleAPIResponse
class SyncExplainRequestBuilder
  def __init__(request: ReqConfig)
  def retry(enabled: bool) → Self
  def execute() → str
class SyncMaybeSingleRequestBuilder
  def __init__(request: ReqConfig)
  def retry(enabled: bool) → Self
  def execute() → SingleAPIResponse|None
class SyncFilterRequestBuilder(BaseFilterRequestBuilder[Client], SyncQueryRequestBuilder)
  def __init__(request: ReqConfig) → None
class SyncRPCFilterRequestBuilder(BaseRPCRequestBuilder, SyncSingleRequestBuilder)
  def __init__(request: ReqConfig) → None
class SyncSelectRequestBuilder(SyncQueryRequestBuilder, BaseSelectRequestBuilder[Client])
  def __init__(request: ReqConfig) → None
  def single() → SyncSingleRequestBuilder
  def maybe_single() → SyncMaybeSingleRequestBuilder
```

### backend/venv/lib/python3.14/site-packages/postgrest/base_client.py
```
class BasePostgrestClient(ABC)
```

### backend/venv/lib/python3.14/site-packages/postgrest/base_request_builder.py
```
class QueryArgs(NamedTuple)
class RequestConfig(Generic[C])
  def send(: RequestConfig[C], additional_headers: Headers)
  def should_retry(response: RequestResponse, attempt_count: int) → bool
class APIResponse(BaseModel) {data*, count?}
class SingleAPIResponse(APIResponse)
class BaseFilterRequestBuilder(Generic[C])
  def __init__(request: RequestConfig[C]) → None
  def not_(: Self) → Self
  def filter(: Self, column: str, operator: str, criteria: str) → Self
  def eq(: Self, column: str, value: Any) → Self
  def neq(: Self, column: str, value: Any) → Self
  def gt(: Self, column: str, value: Any) → Self
  def gte(: Self, column: str, value: Any) → Self
  def lt(: Self, column: str, value: Any) → Self
class BaseSelectRequestBuilder(BaseFilterRequestBuilder[C])
  def limit(: Self, size: int, *, foreign_table: str?) → Self
  def offset(: Self, size: int) → Self
class BaseRPCRequestBuilder(BaseSelectRequestBuilder)
  def single() → Self
  def maybe_single() → Self
  def csv() → Self
def pre_select(*columns: str, count: CountMethod?, head: bool?) → QueryArgs
def pre_insert(json: JSON, *, count: CountMethod?, returning: ReturnMethod, upsert: bool, default_to_null: bool) → QueryArgs
def pre_upsert(json: JSON, *, count: CountMethod?, returning: ReturnMethod, ignore_duplicates: bool, on_conflict: str, default_to_null: bool) → QueryArgs
```

### backend/venv/lib/python3.14/site-packages/postgrest/exceptions.py
```
class APIErrorFromJSON(BaseModel) {message?, code?, hint?, details?}
class APIError(Exception)
  def __init__(error: Dict[str, Any]) → None
  def json() → Dict[str, str]
def generate_default_error_message(r)
```

### backend/venv/lib/python3.14/site-packages/postgrest/types.py
```
class CountMethod(StrEnum)
class Filters(StrEnum)
class RequestMethod(StrEnum)
class ReturnMethod(StrEnum)
```

### backend/venv/lib/python3.14/site-packages/postgrest/utils.py
```
class SyncClient(BaseClient)
  def __init__(*args, **kwargs) → None
  def aclose() → None
def sanitize_param(param: Any) → str
def sanitize_pattern_param(pattern: str) → str
def is_http_url(url: URL) → bool
def model_validate_json(model: Type[TBaseModel], contents) → TBaseModel  # Compatibility layer between pydantic 1 and 2 for parsing an 
```

### backend/venv/lib/python3.14/site-packages/supabase/_async/auth_client.py
```
class AsyncSupabaseAuthClient(AsyncGoTrueClient)
```

### backend/venv/lib/python3.14/site-packages/supabase/_async/client.py
```
class SupabaseException(Exception)
  def __init__(message: str) → None
class AsyncClient
  def table(table_name: str) → AsyncRequestBuilder
  def schema(schema: str) → AsyncPostgrestClient
  def from_(table_name: str) → AsyncRequestBuilder
  def postgrest() → AsyncPostgrestClient
  def storage() → AsyncStorageClient
  def functions() → AsyncFunctionsClient
  def get_channels() → List[AsyncRealtimeChannel]
  async def remove_channel(channel: AsyncRealtimeChannel) → None
async def create_client(supabase_url: str, supabase_key: str, options: ClientOptions?) → AsyncClient
```

### backend/venv/lib/python3.14/site-packages/supabase/_sync/auth_client.py
```
class SyncSupabaseAuthClient(SyncGoTrueClient)
```

### backend/venv/lib/python3.14/site-packages/supabase/_sync/client.py
```
class SupabaseException(Exception)
  def __init__(message: str) → None
class Client
  def table(table_name: str) → SyncRequestBuilder
  def schema(schema: str) → SyncPostgrestClient
  def from_(table_name: str) → SyncRequestBuilder
  def postgrest() → SyncPostgrestClient
  def storage() → SyncStorageClient
  def functions() → SyncFunctionsClient
  def get_channels() → List[SyncRealtimeChannel]
  def remove_channel(channel: SyncRealtimeChannel) → None
def create_client(supabase_url: str, supabase_key: str, options: ClientOptions?) → Client
```

### backend/venv/lib/python3.14/site-packages/supabase/lib/client_options.py
```
@dataclass ClientOptions(schema, headers, auto_refresh_token, persist_session, realtime?, postgrest_client_timeout)
@dataclass AsyncClientOptions(storage, httpx_client?)
@dataclass SyncClientOptions(storage, httpx_client?)
```

### backend/venv/lib/python3.14/site-packages/supabase/types.py
```
class RealtimeClientOptions(TypedDict, total=False)
```

### backend/venv/lib/python3.14/site-packages/supabase-2.31.0.dist-info/METADATA
```
def test_func():
```

### backend/venv/lib/python3.14/site-packages/_distutils_hack/__init__.py
```
class _TrivialRe
  def __init__(*patterns)
  def match(string)
class DistutilsMetaFinder
  def find_spec(fullname, path, target)
  def spec_for_distutils()
  def create_module(spec)
  def exec_module(module)
  def is_cpython()
  def spec_for_pip()
  def pip_imported_during_build()
  def frame_file_is_setup(frame)
class shim
def warn_distutils_present()
def clear_distutils()
def enabled()  # Allow selection of distutils by environment variable
def ensure_local_distutils()
def do_override()  # Ensure that the local copy of distutils is preferred over st
def add_shim()
def insert_shim()
```

### backend/venv/lib/python3.14/site-packages/pkg_resources/__init__.py
```
class PEP440Warning(RuntimeWarning)
class ResolutionError(Exception)
class VersionConflict(ResolutionError)
  def dist()
  def req()
  def report()
  def with_context(required_by)
class ContextualVersionConflict(VersionConflict)
  def required_by()
class DistributionNotFound(ResolutionError)
  def req()
  def requirers()
  def requirers_str()
  def report()
class UnknownExtra(ResolutionError)
class IMetadataProvider(Protocol)
  def has_metadata(name) → bool
  def get_metadata(name)
  def get_metadata_lines(name)
def get_supported_platform()
def register_loader_type(loader_type, provider_factory)  # Register `provider_factory` to make providers for `loader_ty
def get_provider(moduleOrReq)  # Return an IResourceProvider for the named module or requirem
def get_build_platform()  # Return this platform's string for platform-specific distribu
def compatible_platforms(provided, required)  # Can code for the `provided` platform run on the `required` p
def get_distribution(dist)  # Return a current distribution object for a Requirement or st
```

### backend/venv/lib/python3.14/site-packages/pkg_resources/_vendor/typing_extensions.py
```
class _DefaultMixin
  def __init__(default)
class TypeVar(typing.TypeVar, _DefaultMixin, _root=True)
class _SpecialForm(typing._Final, _root=True)
  def __init__(getitem)
def IntVar(name)
```

### backend/venv/lib/python3.14/site-packages/pkg_resources/_vendor/zipp.py
```
class CompleteDirs(zipfile.ZipFile)
  def namelist()
  def resolve_dir(name)
  def make(source)
class FastLookup(CompleteDirs)
  def namelist()
class Path
  def __init__(root, at)
  def open(mode, *args, pwd, **kwargs)
  def name()
  def suffix()
  def suffixes()
  def stem()
  def filename()
  def read_text(*args, **kwargs)
```

### backend/venv/lib/python3.14/site-packages/pkg_resources/extern/__init__.py
```
class VendorImporter
  def search_path()
  def load_module(fullname)
  def create_module(spec)
  def exec_module(module)
  def find_spec(fullname, path, target)
  def install()
```

### backend/venv/lib/python3.14/site-packages/setuptools/__init__.py
```
class Command(_Command)
  def __init__(dist: Distribution, **kw)
  def ensure_string_list(option)
  def reinitialize_command(command, reinit_subcommands, **kw)
class sic(str)
def setup(**attrs)
def findall(dir)  # Find all files under 'dir' and return the list of full filen
def convert_path(pathname)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_core_metadata.py
```
def get_metadata_version()
def rfc822_unescape(content: str) → str  # Reverse RFC-822 escaping by removing leading whitespaces fro
def read_pkg_file(file)  # Reads the metadata values from a file object
def single_line(val)  # Quick and dirty validation for Summary pypa/setuptools#1390
def write_pkg_info(base_dir)  # Write the PKG-INFO file into the release tree
def write_pkg_file(file)
def get_fullname()
```

### backend/venv/lib/python3.14/site-packages/setuptools/_entry_points.py
```
def ensure_valid(ep)  # Exercise one of the dynamic properties to trigger
def load_group(value, group)  # Given a value of an entry point or series of entry points,
def by_group_and_name(ep)
def validate(eps: metadata.EntryPoints)  # Ensure entry points are unique by group and name and validat
def load(eps)  # Given a Distribution
def render(eps: metadata.EntryPoints)
def render_items(eps)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_imp.py
```
def find_spec(module, paths)
def find_module(module, paths)  # Just like 'imp
def get_frozen_object(module, paths)
def get_module(module, paths, info)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_importlib.py
```
def disable_importlib_metadata_finder(metadata)  # Ensure importlib_metadata doesn't provide older, incompatibl
```

### backend/venv/lib/python3.14/site-packages/setuptools/_itertools.py
```
def ensure_unique(iterable, key)  # Wrap an iterable to raise a ValueError if non-unique values 
```

### backend/venv/lib/python3.14/site-packages/setuptools/_normalization.py
```
def safe_identifier(name: str) → str  # Make a string safe to be used as Python identifier
def safe_name(component: str) → str  # Escape a component used as a project name according to Core 
def safe_version(version: str) → str  # Convert an arbitrary string into a valid version string
def best_effort_version(version: str) → str  # Convert an arbitrary string into a version-like string
def safe_extra(extra: str) → str  # Normalize extra name according to PEP 685
def filename_component(value: str) → str  # Normalize each component of a filename (e
def filename_component_broken(value: str) → str  # Produce the incorrect filename component for compatibility
def safer_name(value: str) → str  # Like ``safe_name`` but can be used as filename component for
def safer_best_effort_version(value: str) → str  # Like ``best_effort_version`` but can be used as filename com
```

### backend/venv/lib/python3.14/site-packages/setuptools/_path.py
```
def ensure_directory(path)  # Ensure that the parent directory of `path` exists
def same_path(p1: StrPath, p2: StrPath) → bool  # Differs from os
def normpath(filename: StrPath) → str  # Normalize a file/dir name for comparison purposes
```

### backend/venv/lib/python3.14/site-packages/setuptools/_reqs.py
```
def parse_strings(strs: _StrOrIter) → Iterator[str]  # Yield requirement strings for each specification in `strs`
def parse(strs: _StrOrIter) → Iterator[Requirement]
def parse(strs: _StrOrIter, parser: Callable[[str], _T]) → Iterator[_T]
def parse(strs, parser)  # Replacement for ``pkg_resources
```

### backend/venv/lib/python3.14/site-packages/setuptools/archive_util.py
```
class UnrecognizedFormat(DistutilsError)
def default_filter(src, dst)  # The default progress/filter callback; returns True for all f
def unpack_archive(filename, extract_dir, progress_filter, drivers)
def unpack_directory(filename, extract_dir, progress_filter)  # "Unpack" a directory, using the same interface as for archiv
def unpack_zipfile(filename, extract_dir, progress_filter)  # Unpack zip `filename` to `extract_dir`
def unpack_tarfile(filename, extract_dir, progress_filter)  # Unpack tar/tar
```

### backend/venv/lib/python3.14/site-packages/setuptools/build_meta.py
```
class SetupRequirementsError(BaseException)
  def __init__(specifiers)
class Distribution(setuptools.dist.Distribution)
  def fetch_build_eggs(specifiers)
  def patch()
class _ConfigSettingsTranslator
class _BuildMetaBackend(_ConfigSettingsTranslator)
  def run_setup(setup_script)
  def get_requires_for_build_wheel(config_settings)
  def get_requires_for_build_sdist(config_settings)
  def build_sdist(sdist_directory, config_settings)
  def get_requires_for_build_editable(config_settings)
class _BuildMetaLegacyBackend(_BuildMetaBackend)
  def run_setup(setup_script)
def no_install_setup_requires()  # Temporarily disable installing setup_requires
def suppress_known_deprecation()
```

### backend/venv/lib/python3.14/site-packages/setuptools/depends.py
```
class Require
  def full_name()
  def version_ok(version)
  def get_version(paths, default)
  def is_present(paths)
  def is_current(paths)
def maybe_close(f)
def get_module_constant(module, symbol, default, paths)  # Find 'module' by searching 'paths', and extract 'symbol'
def extract_constant(code, symbol, default)  # Extract the constant value of 'symbol' from 'code'
```

### backend/venv/lib/python3.14/site-packages/setuptools/discovery.py
```
class _Filter
  def __init__(*patterns: str)
class _Finder
class PackageFinder(_Finder)
class PEP420PackageFinder(PackageFinder)
class ModuleFinder(_Finder)
class FlatLayoutPackageFinder(PEP420PackageFinder)
class FlatLayoutModuleFinder(ModuleFinder)
class ConfigDiscovery
  def __init__(distribution: "Distribution")
  def analyse_name()
def remove_nested_packages(packages: List[str]) → List[str]  # Remove nested packages from a list of packages
def remove_stubs(packages: List[str]) → List[str]  # Remove type stubs (:pep:`561`) from a list of packages
def find_parent_package(packages: List[str], package_dir: Mapping[str, str], root_dir: StrPath) → str|None  # Find the parent package that is not a namespace
def find_package_path(name: str, package_dir: Mapping[str, str], root_dir: StrPath) → str
def construct_package_dir(packages: List[str], package_path: StrPath) → Dict[str, str]
```

### backend/venv/lib/python3.14/site-packages/setuptools/dist.py
```
class Distribution(_Distribution)
  def patch_missing_pkg_info(attrs)
  def __init__(attrs: MutableMapping?) → None
  def warn_dash_deprecation(opt, section)
  def make_option_lowercase(opt, section)
  def parse_config_files(filenames, ignore_option_errors)
  def fetch_build_eggs(requires)
  def finalize_options()
  def by_order(hook)
class DistDeprecationWarning(SetuptoolsDeprecationWarning)
def check_importable(dist, attr, value)
def assert_string_list(dist, attr, value)  # Verify that value is a string list
def check_nsp(dist, attr, value)  # Verify that namespace packages are valid
def check_extras(dist, attr, value)  # Verify that extras_require mapping is valid
def assert_bool(dist, attr, value)  # Verify that value is True, False, 0, or 1
def invalid_unless_false(dist, attr, value)
def check_requirements(dist, attr, value)  # Verify that install_requires is a valid requirements list
def check_specifier(dist, attr, value)  # Verify that value is a valid version specifier
def check_entry_points(dist, attr, value)  # Verify that entry_points map is parseable
def check_test_suite(dist, attr, value)
def check_package_data(dist, attr, value)  # Verify that value is a dictionary of package names to glob l
def check_packages(dist, attr, value)
```

### backend/venv/lib/python3.14/site-packages/setuptools/errors.py
```
class InvalidConfigError(OptionError)
class RemovedConfigError(OptionError)
class RemovedCommandError(BaseError, RuntimeError)
class PackageDiscoveryError(BaseError, RuntimeError)
```

### backend/venv/lib/python3.14/site-packages/setuptools/extension.py
```
class Extension(_Extension)
  def __init__(name, sources, *args, **kw)
class Library(Extension)
```

### backend/venv/lib/python3.14/site-packages/setuptools/glob.py
```
def glob(pathname, recursive)  # Return a list of paths matching a pathname pattern
def iglob(pathname, recursive)  # Return an iterator which yields the paths matching a pathnam
def glob1(dirname, pattern)
def glob0(dirname, basename)
def glob2(dirname, pattern)
def has_magic(s)
def escape(pathname)  # Escape all special characters
```

### backend/venv/lib/python3.14/site-packages/setuptools/installer.py
```
class _DeprecatedInstaller(SetuptoolsDeprecationWarning)
def fetch_build_egg(dist, req)  # Fetch an egg needed for building
def strip_marker(req)  # Return a new requirement without the environment marker to a
```

### backend/venv/lib/python3.14/site-packages/setuptools/launch.py
```
def run()  # Run the script in sys
```

### backend/venv/lib/python3.14/site-packages/setuptools/logging.py
```
def configure()  # Configure logging to emit warning and above to stderr
def set_threshold(level)
```

### backend/venv/lib/python3.14/site-packages/setuptools/monkey.py
```
def get_unpatched(item: _T) → _T
def get_unpatched_class()  # Protect against re-patching the distutils if reloaded
def patch_all()
def patch_func(replacement, target_mod, func_name)  # Patch func_name in target_mod with replacement
def get_unpatched_function(candidate)
def patch_for_msvc_specialized_compiler()  # Patch functions in distutils to use standalone Microsoft Vis
```

### backend/venv/lib/python3.14/site-packages/setuptools/msvc.py
```
class PlatformInfo
  def __init__(arch)
  def target_cpu()
  def target_is_x86()
  def current_is_x86()
  def current_dir(hidex86, x64)
  def target_dir(hidex86, x64)
  def cross_dir(forcex86)
class RegistryInfo
  def __init__(platform_info)
  def visualstudio()
  def sxs()
  def vc()
  def vs()
  def vc_for_python()
  def microsoft_sdk()
  def windows_sdk()
class SystemInfo
  def __init__(registry_info, vc_ver)
  def find_reg_vs_vers()
  def find_programdata_vs_vers()
  def VSInstallDir()
  def VCInstallDir()
  def WindowsSdkVersion()
  def WindowsSdkLastVersion()
```

### backend/venv/lib/python3.14/site-packages/setuptools/namespaces.py
```
class Installer
  def install_namespaces()
  def uninstall_namespaces()
class DevelopInstaller(Installer)
```

### backend/venv/lib/python3.14/site-packages/setuptools/package_index.py
```
class ContentChecker
  def feed(block)
  def is_valid()
  def report(reporter, template)
class HashChecker(ContentChecker)
  def __init__(hash_name, expected)
  def from_url(url)
  def feed(block)
  def is_valid()
  def report(reporter, template)
class PackageIndex(Environment)
  def add(dist)
  def process_url(url, retrieve)
  def process_filename(fn, nested)
  def url_ok(url, fatal)
  def scan_egg_links(search_path)
  def scan_egg_link(path, entry)
  def process_index(url, page)
  def need_version_info(url)
class Credential
  def __init__(username, password)
class PyPIConfig(configparser.RawConfigParser)
  def __init__()
  def creds_by_repository()
  def find_credential(url)
```

### backend/venv/lib/python3.14/site-packages/setuptools/sandbox.py
```
class UnpickleableException(Exception)
  def dump(type, exc)
class ExceptionSaver
  def resume()
class AbstractSandbox
  def __init__()
  def run(func)
  def wrap(src, dst, *args, **kw)
  def wrap(path, *args, **kw)
  def wrap(path, *args, **kw)
  def wrap(*args, **kw)
class DirectorySandbox(AbstractSandbox)
  def __init__(sandbox, exceptions)
  def tmpnam()
  def open(file, flags, mode, *args, **kw)
class SandboxViolation(DistutilsError)
def save_argv(repl)
def save_path()
def override_temp(replacement)  # Monkey-patch tempfile
def pushd(target)
def save_modules()  # Context in which imported modules are saved
def save_pkg_resources_state()
def setup_context(setup_dir)
def hide_setuptools()  # Remove references to setuptools' modules from sys
def run_setup(setup_script, args)  # Run a distutils setup script, sandboxed in its directory
```

### backend/venv/lib/python3.14/site-packages/setuptools/unicode_utils.py
```
def decompose(path)
def filesys_decode(path)  # Ensure that the given path is decoded,
def try_encode(string, enc)
```

### backend/venv/lib/python3.14/site-packages/setuptools/warnings.py
```
class SetuptoolsWarning(UserWarning)
class InformationOnly(SetuptoolsWarning)
class SetuptoolsDeprecationWarning(SetuptoolsWarning)
```

### backend/venv/lib/python3.14/site-packages/setuptools/wheel.py
```
class Wheel
  def __init__(filename)
  def tags()
  def is_compatible()
  def egg_name()
  def get_dist_info(zf)
  def install_as_egg(destination_eggdir)
  def get_metadata(name)
  def raw_req(req)
def unpack(src_dir, dst_dir)  # Move everything under `src_dir` to `dst_dir`, and delete the
def disable_info_traces()  # Temporarily disable info traces
```

### backend/venv/lib/python3.14/site-packages/setuptools/windows_support.py
```
def windows_only(func)
def hide_file(path)  # Set the hidden attribute on a file or directory
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/_collections.py
```
class DictStack(list, collections.abc.Mapping)
class RangeMap(dict)
  def left(source)
  def get(key, default)
  def bounds()
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/_functools.py
```
def pass_none(func)  # Wrap func so it's not called if its first param is None
def splat(func)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/_macos_compat.py
```
def bypass_compiler_fixup(cmd, args)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/_modified.py
```
def newer(source, target)  # Is source modified more recently than target
def newer_pairwise(sources, targets, newer)  # Filter filenames where sources are newer than targets
def newer_group(sources, target, missing)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/_msvccompiler.py
```
class MSVCCompiler(CCompiler)
  def __init__(verbose, dry_run, force)
  def initialize(plat_name)
  def out_extensions()
  def spawn(cmd)
  def library_dir_option(dir)
  def runtime_library_dir_option(dir)
  def library_option(lib)
  def find_library_file(dirs, lib, debug)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/archive_util.py
```
def make_tarball(base_name, base_dir, compress, verbose, dry_run, owner, group)  # Create a (possibly compressed) tar file from all the files u
def make_zipfile(base_name, base_dir, verbose, dry_run)
def check_archive_formats(formats)  # Returns the first format from the 'format' list that is unkn
def make_archive(base_name, format, root_dir, base_dir, verbose, dry_run, owner, group)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/bcppcompiler.py
```
class BCPPCompiler(CCompiler)
  def __init__(verbose, dry_run, force)
  def find_library_file(dirs, lib, debug)
  def object_filenames(source_filenames, strip_dir, output_dir)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/ccompiler.py
```
class CCompiler
  def __init__(verbose, dry_run, force)
  def set_executables(**kwargs)
  def set_executable(key, value)
  def define_macro(name, value)
  def undefine_macro(name)
  def add_include_dir(dir)
  def set_include_dirs(dirs)
  def add_library(libname)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/cmd.py
```
class Command
  def __init__(dist)
  def ensure_finalized()
  def initialize_options()
  def finalize_options()
  def dump_options(header, indent)
  def run()
  def announce(msg, level)
  def debug_print(msg)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/config.py
```
class PyPIRCCommand(Command)
  def initialize_options()
  def finalize_options()
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/core.py
```
def gen_usage(script_name)
def setup(**attrs)
def run_commands(dist)  # Given a Distribution object run all the commands,
def run_setup(script_name, script_args, stop_after)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/cygwinccompiler.py
```
class CygwinCCompiler(UnixCCompiler)
  def __init__(verbose, dry_run, force)
  def gcc_version()
  def runtime_library_dir_option(dir)
  def out_extensions()
class Mingw32CCompiler(CygwinCCompiler)
  def __init__(verbose, dry_run, force)
  def runtime_library_dir_option(dir)
def get_msvcr()  # Include the appropriate MSVC runtime library if Python was b
def check_config_h()
def is_cygwincc(cc)  # Try to determine if the compiler that would be used is from 
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/dir_util.py
```
def mkpath(name, mode, verbose, dry_run)
def create_tree(base_dir, files, mode, verbose, dry_run)  # Create all the empty directories under 'base_dir' needed to 
def copy_tree(src, dst, preserve_mode, preserve_times, preserve_symlinks, update, verbose, dry_run)
def remove_tree(directory, verbose, dry_run)  # Recursively remove an entire directory tree
def ensure_relative(path)  # Take the full path 'path', and make it a relative path
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/dist.py
```
class Distribution
  def __init__(attrs)
  def get_option_dict(command)
  def dump_option_dicts(header, commands, indent)
  def find_config_files()
  def parse_config_files(filenames)
  def parse_command_line()
  def finalize_options()
  def handle_display_options(option_order)
class DistributionMetadata
  def __init__(path)
  def read_pkg_file(file)
  def write_pkg_info(base_dir)
  def write_pkg_file(file)
  def maybe_write(header, val)
  def get_name()
  def get_version()
  def get_fullname()
def fix_help_options(options)  # Convert a 4-tuple 'help_options' list as found in various co
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/errors.py
```
class DistutilsError(Exception)
class DistutilsModuleError(DistutilsError)
class DistutilsClassError(DistutilsError)
class DistutilsGetoptError(DistutilsError)
class DistutilsArgError(DistutilsError)
class DistutilsFileError(DistutilsError)
class DistutilsOptionError(DistutilsError)
class DistutilsSetupError(DistutilsError)
class DistutilsPlatformError(DistutilsError)
class DistutilsExecError(DistutilsError)
class DistutilsInternalError(DistutilsError)
class DistutilsTemplateError(DistutilsError)
class DistutilsByteCompileError(DistutilsError)
class CCompilerError(Exception)
class PreprocessError(CCompilerError)
class CompileError(CCompilerError)
class LibError(CCompilerError)
class LinkError(CCompilerError)
class UnknownFileError(CCompilerError)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/extension.py
```
class Extension
def read_setup_file(filename)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/fancy_getopt.py
```
class FancyGetopt
  def __init__(option_table)
  def set_option_table(option_table)
  def add_option(long_option, short_option, help_string)
  def has_option(long_option)
  def get_attr_name(long_option)
  def set_aliases(alias)
  def set_negative_aliases(negative_alias)
  def getopt(args, object)
class OptionDummy
  def __init__(options: Sequence[Any])
def fancy_getopt(options, negative_opt, object, args)
def wrap_text(text, width)  # wrap_text(text : string, width : int) -> [string]
def translate_longopt(opt)  # Convert a long option name to a valid Python identifier by
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/file_util.py
```
def copy_file(src, dst, preserve_mode, preserve_times, update, link, verbose, dry_run)
def move_file(src, dst, verbose, dry_run)
def write_file(filename, contents)  # Create a file with the specified name and write 'contents' (
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/filelist.py
```
class FileList
  def __init__(warn, debug_print)
  def set_allfiles(allfiles)
  def findall(dir)
  def debug_print(msg)
  def append(item)
  def extend(items)
  def sort()
  def remove_duplicates()
class _UniqueDirs(set)
  def filter(items)
def findall(dir)  # Find all files under 'dir' and return the list of full filen
def glob_to_re(pattern)  # Translate a shell-like glob pattern to a regular expression;
def translate_pattern(pattern, anchor, prefix, is_regex)  # Translate a shell-like wildcard pattern to a compiled regula
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/log.py
```
class Log(logging.Logger)
  def __init__(threshold)
  def threshold()
  def threshold(level)
def set_threshold(level)
def set_verbosity(v)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/msvc9compiler.py
```
class Reg
  def get_value(path, key)
  def read_keys(base, key)
  def read_values(base, key)
  def convert_mbcs(s)
class MacroExpander
  def __init__(version)
  def set_macro(macro, path, key)
  def load_macros(version)
  def sub(s)
class MSVCCompiler(CCompiler)
  def __init__(verbose, dry_run, force)
  def initialize(plat_name)
  def object_filenames(source_filenames, strip_dir, output_dir)
  def manifest_setup_ldargs(output_filename, build_temp, ld_args)
  def manifest_get_embed_info(target_desc, ld_args)
  def library_dir_option(dir)
  def runtime_library_dir_option(dir)
  def library_option(lib)
def get_build_version()  # Return the version of MSVC that was used to build Python
def normalize_and_reduce_paths(paths)  # Return a list of normalized paths with duplicates removed
def removeDuplicates(variable)  # Remove duplicate values of an environment variable
def find_vcvarsall(version)  # Find the vcvarsall
def query_vcvarsall(version, arch)  # Launch vcvarsall
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/msvccompiler.py
```
class MacroExpander
  def __init__(version)
  def set_macro(macro, path, key)
  def load_macros(version)
  def sub(s)
class MSVCCompiler(CCompiler)
  def __init__(verbose, dry_run, force)
  def initialize()
  def object_filenames(source_filenames, strip_dir, output_dir)
  def library_dir_option(dir)
  def runtime_library_dir_option(dir)
  def library_option(lib)
  def find_library_file(dirs, lib, debug)
  def find_exe(exe)
def read_keys(base, key)  # Return list of registry keys
def read_values(base, key)  # Return dict of registry keys and values
def convert_mbcs(s)
def get_build_version()  # Return the version of MSVC that was used to build Python
def get_build_architecture()  # Return the processor architecture
def normalize_and_reduce_paths(paths)  # Return a list of normalized paths with duplicates removed
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/py38compat.py
```
def aix_platform(osname, version, release)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/py39compat.py
```
class UnequalIterablesError(ValueError)
  def __init__(details)
def add_ext_suffix_39(vars)  # Ensure vars contains 'EXT_SUFFIX'
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/spawn.py
```
def spawn(cmd, search_path, verbose, dry_run, env)
def find_executable(executable, path)  # Tries to find 'executable' in the directories listed in 'pat
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/sysconfig.py
```
def get_python_version()  # Return a string containing the major and minor Python versio
def get_python_inc(plat_specific, prefix)  # Return the directory containing installed Python header file
def get_python_lib(plat_specific, standard_lib, prefix)
def customize_compiler(compiler)
def get_config_h_filename()  # Return full pathname of installed pyconfig
def get_makefile_filename()  # Return full pathname of installed Makefile from the Python b
def parse_config_h(fp, g)  # Parse a config
def parse_makefile(fn, g)
def expand_makefile_vars(s, vars)  # Expand Makefile-style variables -- "${foo}" or "$(foo)" -- i
def get_config_vars(*args)  # With no arguments, return a dictionary of all configuration
def get_config_var(name)  # Return the value of a single variable using the dictionary
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/text_file.py
```
class TextFile
  def __init__(filename, file, **options)
  def open(filename)
  def close()
  def gen_error(msg, line)
  def error(msg, line)
  def warn(msg, line)
  def readline()
  def readlines()
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/unixccompiler.py
```
class UnixCCompiler(CCompiler)
  def library_dir_option(dir)
  def runtime_library_dir_option(dir: str) → str | list[str]
  def library_option(lib)
  def find_library_file(dirs, lib, debug)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/util.py
```
def get_host_platform()  # Return a string that identifies the current platform
def get_platform()
def get_macosx_target_ver_from_syscfg()  # Get the version of macOS latched in the Python interpreter c
def get_macosx_target_ver()  # Return the version of macOS for which we are building
def split_version(s)  # Convert a dot-separated string into a list of numbers for co
def convert_path(pathname)  # Return 'pathname' as a name that will work on the native fil
def change_root(new_root, pathname)  # Return 'pathname' with 'new_root' prepended
def check_environ()  # Ensure that 'os
def subst_vars(s, local_vars)  # Perform variable substitution on 'string'
def grok_environment_error(exc, prefix)
def split_quoted(s)  # Split a string up according to Unix shell-like rules for quo
def execute(func, args, msg, verbose, dry_run)  # Perform some action that affects the outside world (eg
def strtobool(val)  # Convert a string representation of truth to true (1) or fals
def byte_compile(py_files, optimize, force, prefix, base_dir, verbose, dry_run, direct)
def rfc822_escape(header)  # Return a version of the string escaped for inclusion in an
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/version.py
```
class Version
  def __init__(vstring)
class StrictVersion(Version)
  def parse(vstring)
class LooseVersion(Version)
  def parse(vstring)
def suppress_known_deprecation()
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/versionpredicate.py
```
class VersionPredicate
  def __init__(versionPredicateStr)
  def satisfied_by(version)
def splitUp(pred)  # Parse a single version comparison
def split_provision(value)  # Return the name and optional version number of a provision
```

### backend/venv/lib/python3.14/site-packages/setuptools/_distutils/zosccompiler.py
```
class zOSCCompiler(UnixCCompiler)
  def __init__(verbose, dry_run, force)
  def runtime_library_dir_option(dir)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_vendor/ordered_set.py
```
class OrderedSet(MutableSet, Sequence)
  def __init__(iterable)
  def copy()
  def add(key)
  def update(sequence)
  def index(key)
  def pop()
  def discard(key)
  def clear()
def is_iterable(obj)  # Are we being asked to look up a list of things, instead of a
```

### backend/venv/lib/python3.14/site-packages/setuptools/_vendor/typing_extensions.py
```
class _ExtensionsGenericMeta(GenericMeta)
def IntVar(name)
```

### backend/venv/lib/python3.14/site-packages/setuptools/_vendor/zipp.py
```
class CompleteDirs(zipfile.ZipFile)
  def namelist()
  def resolve_dir(name)
  def make(source)
class FastLookup(CompleteDirs)
  def namelist()
class Path
  def __init__(root, at)
  def open(mode, *args, pwd, **kwargs)
  def name()
  def suffix()
  def suffixes()
  def stem()
  def filename()
  def read_text(*args, **kwargs)
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/_requirestxt.py
```
def write_requirements(cmd, basename, filename)
def write_setup_requirements(cmd, basename, filename)
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/alias.py
```
class alias(option_base)
  def initialize_options()
  def finalize_options()
  def run()
def shquote(arg)  # Quote an argument for later parsing by shlex
def format_alias(name, aliases)
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/bdist_egg.py
```
class bdist_egg(Command)
  def initialize_options()
  def finalize_options()
  def do_install_data()
  def get_outputs()
  def call_command(cmdname, **kw)
  def run()
  def zap_pyfiles()
  def zip_safe()
def strip_module(filename)
def sorted_walk(dir)  # Do os
def write_stub(resource, pyfile)
def walk_egg(egg_dir)  # Walk an unpacked egg's contents, skipping the metadata direc
def analyze_egg(egg_dir, stubs)
def write_safety_flag(egg_dir, safe)
def scan_module(egg_dir, base, name, stubs)  # Check whether module possibly uses unsafe-for-zipfile stuff
def iter_symbols(code)  # Yield names and strings used by `code` and its nested code o
def can_scan()
def make_zipfile(zip_filename, base_dir, verbose, dry_run, compress, mode)  # Create a zip file from all the files under 'base_dir'
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/bdist_rpm.py
```
class bdist_rpm(orig.bdist_rpm)
  def run()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/build.py
```
class build(_build)
  def get_sub_commands()
class SubCommand(Protocol)
  def initialize_options()
  def finalize_options()
  def run()
  def get_source_files() → List[str]
  def get_outputs() → List[str]
  def get_output_mapping() → Dict[str, str]
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/build_clib.py
```
class build_clib(orig.build_clib)
  def build_libraries(libraries)
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/build_ext.py
```
class build_ext(_build_ext)
  def run()
  def copy_extensions_to_source()
  def get_ext_filename(fullname)
  def initialize_options()
  def finalize_options()
  def setup_shlib_compiler()
  def get_export_symbols(ext)
  def build_extension(ext)
def if_dl(s)
def get_abi3_suffix()  # Return the file extension for an abi3-compliant Extension()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/build_py.py
```
class build_py(orig.build_py)
  def finalize_options()
  def run()
  def build_module(module, module_file, package)
  def get_data_files_without_manifest()
  def find_data_files(package, src_dir)
  def get_outputs(include_bytecode) → List[str]
  def get_output_mapping() → Dict[str, str]
  def build_package_data()
class _IncludePackageDataAbuse
  def __init__()
  def is_module(file)
  def importable_subpackage(parent, file)
  def warn(importable)
def make_writable(target)
def assert_relative(path)
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/develop.py
```
class develop(namespaces.DevelopInstaller, easy_install)
  def run()
  def initialize_options()
  def finalize_options()
  def install_for_development()
  def uninstall_link()
  def install_egg_scripts(dist)
  def install_wrapper_scripts(dist)
class VersionlessRequirement
  def __init__(dist)
  def as_requirement()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/dist_info.py
```
class dist_info(Command)
  def initialize_options()
  def finalize_options()
  def run()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/easy_install.py
```
class easy_install(Command)
  def initialize_options()
  def delete_blockers(blockers)
  def finalize_options()
  def expand_basedirs()
  def expand_dirs()
  def run(show_deprecation)
  def pseudo_tempname()
  def warn_deprecated_options()
class PthDistributions(Environment)
  def save()
  def add(dist)
  def remove(dist)
  def make_relative(path)
class RewritePthDistributions(PthDistributions)
class WindowsScriptWriter(ScriptWriter)
  def best()
class WindowsExecutableLauncherWriter(WindowsScriptWriter)
class EasyInstallDeprecationWarning(SetuptoolsDeprecationWarning)
def is_64bit()
def isascii(s)
def get_site_dirs()  # Return a list of 'site' dirs
def expand_paths(inputs)
def extract_wininst_cfg(dist_filename)  # Extract configuration data from a bdist_wininst
def get_exe_prefixes(exe_filename)  # Get exe->egg path translations for a given
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/editable_wheel.py
```
class _EditableMode(Enum)
  def convert(mode: str?) → "_EditableMode"
class editable_wheel(Command)
  def initialize_options()
  def finalize_options()
  def run()
class EditableStrategy(Protocol)
class _StaticPth
  def __init__(dist: Distribution, name: str, path_entries: List[Path])
class _LinkTree(_StaticPth)
class _TopLevelFinder
  def __init__(dist: Distribution, name: str)
class _NamespaceInstaller(namespaces.Installer)
  def __init__(distribution, installation_dir, editable_name, src_root)
class LinksNotSupported(errors.FileError)
class _DebuggingTips(SetuptoolsWarning)
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/egg_info.py
```
class InfoCommon
  def name()
  def tagged_version()
  def tags() → str
class egg_info(InfoCommon, Command)
  def initialize_options()
  def tag_svn_revision()
  def tag_svn_revision(value)
  def save_version_info(filename)
  def finalize_options()
  def write_or_delete_file(what, filename, data, force)
  def write_file(what, filename, data)
  def delete_file(filename)
class FileList(_FileList)
  def __init__(warn, debug_print, ignore_egg_info_dir)
  def process_template_line(line)
  def include(pattern)
  def exclude(pattern)
  def recursive_include(dir, pattern)
  def recursive_exclude(dir, pattern)
  def graft(dir)
  def prune(dir)
class manifest_maker(sdist)
  def initialize_options()
  def finalize_options()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/install.py
```
class install(orig.install)
  def initialize_options()
  def finalize_options()
  def handle_extra_path()
  def run()
  def do_egg_install()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/install_egg_info.py
```
class install_egg_info(namespaces.Installer, Command)
  def initialize_options()
  def finalize_options()
  def run()
  def get_outputs()
  def copytree()
  def skimmer(src, dst)
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/install_lib.py
```
class install_lib(orig.install_lib)
  def run()
  def get_exclusions()
  def pf(src, dst)
  def get_outputs()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/install_scripts.py
```
class install_scripts(orig.install_scripts)
  def initialize_options()
  def run()
  def write_script(script_name, contents, mode, *ignored)
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/launcher manifest.xml
```
root assembly
assemblyIdentity[name=%(name)s]
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/register.py
```
class register(orig.register)
  def run()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/rotate.py
```
class rotate(Command)
  def initialize_options()
  def finalize_options()
  def run()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/saveopts.py
```
class saveopts(option_base)
  def run()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/sdist.py
```
class sdist(orig.sdist)
  def run()
  def initialize_options()
  def make_distribution()
  def add_defaults()
  def check_readme()
  def make_release_tree(base_dir, files)
  def read_manifest()
def walk_revctrl(dirname)  # Find all files under revision control
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/setopt.py
```
class option_base(Command)
  def initialize_options()
  def finalize_options()
class setopt(option_base)
  def initialize_options()
  def finalize_options()
  def run()
def config_file(kind)  # Get the filename of the distutils, local, global, or per-use
def edit_config(filename, settings, dry_run)  # Edit a configuration file to include `settings`
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/test.py
```
class ScanningLoader(TestLoader)
  def __init__()
  def loadTestsFromModule(module, pattern)
class NonDataProperty
  def __init__(fget)
class test(Command)
  def initialize_options()
  def finalize_options()
  def test_args()
  def with_project_on_sys_path(func)
  def paths_on_pythonpath(paths)
  def install_dists(dist)
  def run()
  def run_tests()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/upload.py
```
class upload(orig.upload)
  def run()
```

### backend/venv/lib/python3.14/site-packages/setuptools/command/upload_docs.py
```
class upload_docs(upload)
  def has_sphinx()
  def initialize_options()
  def finalize_options()
  def create_zipfile(filename)
  def run()
  def upload_file(filename)
```

### backend/venv/lib/python3.14/site-packages/setuptools/compat/py311.py
```
def shutil_rmtree(path, ignore_errors, onexc)
```

### backend/venv/lib/python3.14/site-packages/setuptools/config/_apply_pyprojecttoml.py
```
def apply(dist: "Distribution", config: dict, filename: StrPath) → "Distribution"  # Apply configuration dict read with :func:`read_configuration
def json_compatible_key(key: str) → str  # As defined in :pep:`566#json-compatible-metadata`
```

### backend/venv/lib/python3.14/site-packages/setuptools/config/expand.py
```
class StaticModule
  def __init__(name: str, spec: ModuleSpec)
class EnsurePackagesDiscovered
  def __init__(distribution: "Distribution")
  def package_dir() → Mapping[str, str]
class LazyMappingProxy(Mapping[_K, _V])
  def __init__(obtain_mapping_value: Callable[[], Mapping[_K, _V]])
def glob_relative(patterns: Iterable[str], root_dir: StrPath?) → List[str]  # Expand the list of glob patterns, but preserving relative pa
def read_files(filepaths: Union[str, bytes, Iterable[StrPath]], root_dir) → str  # Return the content of the files concatenated using ``\n`` as
def read_attr(attr_desc: str, package_dir: Optional[Mapping[str, str]], root_dir: StrPath?)
def resolve_class(qualified_class_name: str, package_dir: Optional[Mapping[str, str]], root_dir: StrPath?) → Callable  # Given a qualified class name, return the associated class ob
def cmdclass(values: Dict[str, str], package_dir: Optional[Mapping[str, str]], root_dir: StrPath?) → Dict[str, Callable]  # Given a dictionary mapping command names to strings for qual
def find_packages(*, namespaces, fill_package_dir: Optional[Dict[str, str]], root_dir: StrPath?, **kwargs) → List[str]
def version(value: Union[Callable, Iterable[Union[str, int]], str]) → str  # When getting the version directly from an attribute,
def canonic_package_data(package_data: dict) → dict
def canonic_data_files(data_files: Union[list, dict], root_dir: StrPath?) → List[Tuple[str, List[str]]]  # For compatibility with ``setup
def entry_points(text: str, text_source) → Dict[str, dict]  # Given the contents of entry-points file,
```

### backend/venv/lib/python3.14/site-packages/setuptools/config/pyprojecttoml.py
```
class _ConfigExpander
  def expand()
class _EnsurePackagesDiscovered(_expand.EnsurePackagesDiscovered)
class _ExperimentalConfiguration(SetuptoolsWarning)
def load_file(filepath: StrPath) → dict
def validate(config: dict, filepath: StrPath) → bool
def apply_configuration(dist: "Distribution", filepath: StrPath, ignore_option_errors) → "Distribution"  # Apply the configuration from a ``pyproject
def read_configuration(filepath: StrPath, expand, ignore_option_errors, dist: "Distribution"?)
def expand_configuration(config: dict, root_dir: StrPath?, ignore_option_errors: bool, dist: "Distribution"?) → dict
```

### backend/venv/lib/python3.14/site-packages/setuptools/config/setupcfg.py
```
class ConfigHandler(Generic[Target])
  def parsers()
  def parser(value)
  def parse(value)
  def parse_section(section_options)
  def parse() → None
  def config_handler(*args, **kwargs)
class ConfigMetadataHandler(ConfigHandler["DistributionMetadata"])
  def parsers()
class ConfigOptionsHandler(ConfigHandler["Distribution"])
  def parsers()
  def parse_section_packages__find(section_options)
  def parse_section_entry_points(section_options)
  def parse_section_package_data(section_options)
  def parse_section_exclude_package_data(section_options)
  def parse_section_extras_require(section_options)
  def parse_section_data_files(section_options)
class _AmbiguousMarker(SetuptoolsDeprecationWarning)
  def message(**kw)
class _DeprecatedConfig(SetuptoolsDeprecationWarning)
def read_configuration(filepath: StrPath, find_others, ignore_option_errors) → dict  # Read given configuration file and returns options from it as
def apply_configuration(dist: "Distribution", filepath: StrPath) → "Distribution"  # Apply the configuration from a ``setup
def configuration_to_dict(handlers: Tuple["ConfigHandler", ...]) → dict  # Returns configuration data gathered by given handlers as a d
def parse_configuration(distribution: "Distribution", command_options: AllCommandOptions, ignore_option_errors) → Tuple["ConfigMetadataHandle...  # Performs additional parsing of configuration options
```

### backend/venv/lib/python3.14/site-packages/setuptools/extern/__init__.py
```
class VendorImporter
  def search_path()
  def load_module(fullname)
  def create_module(spec)
  def exec_module(module)
  def find_spec(fullname, path, target)
  def install()
```

### backend/delete_enrollments_temp.py
```
def delete_enrollment_records()
```

### backend/reset_db_temp.py
```
def reset_database()
```

### backend/seed_real_faces.py
```
def download_url(url)  # Download bytes from URL with user-agent headers
def process_user(user, admin_id)  # Downloads image and extracts the real face embedding
def seed_real_faces()
```

### backend/download_test_images.py
```
def download_image(student, dest_folder)
def main()
```

### backend/Dockerfile
```
FROM python:3.11-slim-bookworm
EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### backend/insert_seed_users.py
```
def seed_users()
```

### backend/main.py
```
def health()  # Application status health-check endpoint
GET /  →  health()
```

### backend/routes/auth.py
```
class LoginRequest(BaseModel) {email*}
class LoginResponse(BaseModel) {token*, user_id*, role*, college_name?}
class TokenPayload(BaseModel) {sub*, role*, exp*}
def create_token(user_id: str, role: str) → str  # Create a signed JWT token with user_id and role
def verify_token(token: str) → dict  # Decode and validate a JWT token
async def login(payload: LoginRequest)  # Authenticate a user by email and return a JWT token
GET /protected  →  my_route()
POST /login  →  login()
```

### backend/routes/outpass.py
```
GET /logs/{admin_user_id}  →  get_logs()
GET /currently-out/{admin_user_id}  →  currently_out()
```

### backend/routes/face.py
```
class EnrollRequest(BaseModel) {student_id?, base64_image?}
class IdentifyRequest(BaseModel) {base64_image?, admin_user_id?, is_front_camera?}
class LogActionRequest(BaseModel) {student_id?, action?, confidence?, logged_by?, gate?}
POST /enroll  →  enroll_face()
POST /identify  →  identify_face()
POST /log-action  →  log_action()
```

### backend/routes/students.py
```
class StudentRegisterRequest(BaseModel) {user_id?, name?, roll_number?, department?, year?, room_number?}
POST /register  →  register_student()
GET /stats/{user_id}  →  get_student_stats()
GET /list/{user_id}  →  list_students()
GET /{student_id}  →  get_student()
GET /{student_id}/status  →  get_student_status()
```

### backend/services/face_engine.py
```
class EmbeddingCache
  def __init__()
  def refresh(supabase_client, college_user_ids: list)
  def is_stale()
  def get()
  def count()
def decode_image(base64_string: str) → np.ndarray  # Convert a base64-encoded image (from the mobile camera) into
def extract_embedding(base64_image: str) → dict  # Extract the 128-dimensional face embedding from a base64-enc
def get_cache()  # Return the global embedding cache singleton
def identify_student_fast(query_base64: str, cache: EmbeddingCache, tolerance: float, is_front_camera: bool) → dict  # FAST 1:N face identification using vectorized numpy distance
```

### backend/test_api.py
```
def get_base64_image(filename: str) → str  # Read a local image file and encode it as base64
def seed_test_admin()  # Ensure a test admin user exists in the Supabase database
def make_request(url: str, method: str, data: dict, headers: dict) → tuple  # Helper using urllib to make HTTP requests and return (status
def run_tests()  # Executes the test suite against the local running FastAPI se
def start_server_and_test()  # Starts the FastAPI server as a background process and trigge
```

## mobile

### mobile/android/app/src/debugOptimized/AndroidManifest.xml
```
root manifest
uses-permission[name=android.permission.SYSTEM_ALERT_WINDOW]
```

### mobile/android/app/src/main/res/drawable/rn_edit_text_material.xml
```
root inset
```

### mobile/android/app/src/main/res/drawable/ic_launcher_background.xml
```
root layer-list
```

### mobile/android/app/src/debug/AndroidManifest.xml
```
root manifest
uses-permission[name=android.permission.SYSTEM_ALERT_WINDOW]
```

### mobile/android/gradle/wrapper/gradle-wrapper.properties
```
key distributionBase
key distributionPath
key distributionUrl
key networkTimeout
key validateDistributionUrl
key zipStoreBase
key zipStorePath
```

### mobile/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
```
root adaptive-icon
```

### mobile/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml
```
root adaptive-icon
```

### mobile/android/app/src/main/AndroidManifest.xml
```
root manifest
uses-permission[name=android.permission.CAMERA]
uses-permission[name=android.permission.INTERNET]
uses-permission[name=android.permission.READ_EXTERNAL_STORAGE]
uses-permission[name=android.permission.RECORD_AUDIO]
uses-permission[name=android.permission.SYSTEM_ALERT_WINDOW]
uses-permission[name=android.permission.VIBRATE]
uses-permission[name=android.permission.WRITE_EXTERNAL_STORAGE]
action[name=android.intent.action.VIEW]
category[name=android.intent.category.BROWSABLE]
application[name=.MainApplication]
meta-data[name=expo.modules.updates.ENABLED]
meta-data[name=expo.modules.updates.ENABLE_BSDIFF_PATCH_SUPPORT]
meta-data[name=expo.modules.updates.EXPO_UPDATES_CHECK_ON_LAUNCH]
meta-data[name=expo.modules.updates.EXPO_UPDATES_LAUNCH_WAIT_MS]
activity[name=.MainActivity]
action[name=android.intent.action.MAIN]
category[name=android.intent.category.LAUNCHER]
```

### mobile/android/gradle.properties
```
group org.gradle
key org.gradle.jvmargs
key org.gradle.parallel
group android.useAndroidX
key android.useAndroidX
group android.enablePngCrunchInReleaseBuilds
key android.enablePngCrunchInReleaseBuilds
key reactNativeArchitectures
key newArchEnabled
key hermesEnabled
key edgeToEdgeEnabled
group expo.gif
key expo.gif.enabled
group expo.webp
key expo.webp.enabled
key expo.webp.animated
key EX_DEV_CLIENT_NETWORK_INSPECTOR
group expo.useLegacyPackaging
key expo.useLegacyPackaging
group expo.inlineModules
key expo.inlineModules.watchedDirectories
```

### mobile/android/app/src/main/res/values/colors.xml
```
root resources
color[name=splashscreen_background]
color[name=iconBackground]
color[name=colorPrimary]
```

### mobile/android/app/src/main/res/values-night/colors.xml
```
root resources
```

### mobile/android/app/src/main/res/values/strings.xml
```
root resources
string[name=app_name]
```

### mobile/android/app/src/main/res/values/styles.xml
```
root resources
style[name=AppTheme]
item[name=android:editTextBackground]
item[name=colorPrimary]
item[name=android:statusBarColor]
item[name=android:navigationBarColor]
style[name=Theme.App.SplashScreen]
item[name=android:windowBackground]
```

### mobile/android/settings.gradle
```
def reactNativeGradlePlugin = new File(
def expoPluginsPath = new File(
```

### mobile/android/app/build.gradle
```
def projectRoot = rootDir.getAbsoluteFile().getParentFile().getAbsolutePath()
def enableMinifyInReleaseBuilds = (findProperty('android.enableMinifyInReleaseBuilds') ?: false).toBoolean()
def jscFlavor = 'io.github.react-native-community:jsc-android:2026004.+'
def enableShrinkResources = findProperty('android.enableShrinkResourcesInReleaseBuilds') ?: 'false'
def enablePngCrunchInRelease = findProperty('android.enablePngCrunchInReleaseBuilds') ?: 'true'
def enableLegacyPackaging = findProperty('expo.useLegacyPackaging') ?: 'false'
def options = (findProperty("android.packagingOptions.$prop") ?: "").split(",");
def isGifEnabled = (findProperty('expo.gif.enabled') ?: "") == "true";
def isWebpEnabled = (findProperty('expo.webp.enabled') ?: "") == "true";
def isWebpAnimatedEnabled = (findProperty('expo.webp.animated') ?: "") == "true";
```

### mobile/android/local.properties
```
group sdk.dir
key sdk.dir
```

### mobile/android/.gradle/buildOutputCleanup/cache.properties
```
group gradle.version
key gradle.version
```

### mobile/AGENTS.md
```
h1 Expo HAS CHANGED
```

### mobile/services/api.js
```
export const login = (email) =>
export const registerStudent = (studentData) =>
export const getStudents = (userId) =>
export const getStudentStats = (userId) =>
export const getStudentDetail = (studentId) =>
export const getStudentStatus = (studentId) =>
export const enrollFace = (studentId, base64Image) =>
export const identifyFace = (base64Image, adminUserId, isFrontCamera = false) =>
export const logAction = (studentId, action, confidence, loggedBy, gate = 'MAIN_GATE') =>
export const getRecentLogs = (adminUserId) =>
export const getCurrentlyOut = (adminUserId) =>
```
